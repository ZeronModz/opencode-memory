# Session: 2026-07-31 — ZerFi v4.1 Bug Fix

## Time: 06:30-06:40
## User: Hasan (DevZeron)

## Bug
Engine.sh output showed `{{IFACE}}` and `{{APP_DIR}}` — template variables from old version.
Root cause: User was running old APK. New engine.sh uses `$1` and `$2` params.

## Fix
1. Added quotes around arguments in ZerFiEngine.java: `'"$appDir" "$iface"'`
2. Clean rebuild to ensure fresh assets in APK
3. Verified engine.sh in APK has correct content (no {{}} templates)

## APK
/data/data/com.termux/files/home/ZerFi.apk (15MB)

# ------------------------------------------------------------
# Session 2: 2026-07-31 — dev-zeron-temp-gmail-api-v1 (Vercel)

## Time: ~07:30-08:00
## User: Hasan (DevZeron)
## Phone: Phone 1 (Samsung A05m)

## Task
Temporary-Gmail-Vercel-Simple project ta full advance rebuild. User ja chaichilo:
1. GMAIL creds code e hardcoded na — shudhu env vars theke (user curl example e jevabe App Password Authorization header e diche).
2. Proti API response e `"Api_By": "@DevZeron"` o `"Tg_Channel": "t.me/CodeDevZeron"`.
3. More advanced features (Vercel-supported).
4. Full documentation website (curl, Python, Node.js, PHP examples).
5. Vercel project name: dev-zeron-temp-gmail-api-v1.

## What I Did
- `api/aliases.py` — custom() + batch() added, generate() supports mixed/random alias.
- `api/index.py` — full rebuild. All secrets from env only. Credit fields in every response. New endpoints: custom alias, batch, unread filter, count, markread, markunread, health, info.
- `public/index.html` — new full docs website (dark dev theme via ui-ux-pro-max skill: #020617 bg, #22C55E accent, Inter + JetBrains Mono). Tabs for curl/Python/Node/PHP + response examples + FAQ.
- `vercel.json` — /api/(.*) → /api/index.py rewrite (static public/ serves docs at root).
- `README.md` — full docs + deploy steps.
- `.env.example` → placeholder; real creds only in gitignored `.env`.
- `tests/test_aliases.py` — 8 tests (dot, plus, mixed, custom, sanitise, batch, clamp).

## Verification
- 8/8 unit tests OK.
- Flask test client: generate mixed/custom/batch OK, noauth → 401, 404 handler OK.
- Live IMAP: /api/read OK (0 msgs), /api/health → "IMAP connected, INBOX reachable".

## Real Creds (used only in .env, gitignored)
- GMAIL_ADDRESS=zeronmodz@gmail.com
- GMAIL_APP_PASSWORD=vqae nhux pqez izqz

## Next
- User GitHub e push korbe (private), Vercel e deploy korbe name: dev-zeron-temp-gmail-api-v1. Env vars: GMAIL_ADDRESS + GMAIL_APP_PASSWORD.

# ------------------------------------------------------------
# Session 3: 2026-07-31 — dev-zeron-temp-gmail-api-v1 (OPEN SOURCE auth + live)

## Time: ~18:30-18:50
## User: Hasan (DevZeron)
## Phone: Phone 1 (Samsung A05m)

## Change: OPEN SOURCE mode
- Auth requirement changed: kono env vars lagbe na. Proti user nijer Gmail + App Password.
- `parse_auth()` in api/index.py supports 3 formats:
  1. `Bearer <email>|<app-password>` (pipe)
  2. `X-Gmail-Address` header + `Bearer <app-password>`
  3. `Bearer <app-password>` → admin mode (GMAIL_ADDRESS env)
- Trash fix: Gmail `\Trash` via X-GM-LABELS, FLAGS rejects it.

## Docs website
- `public/index.html` fully rewritten. data-copy attr regex `.*` corrupted HTML → fresh rewrite. Copy buttons now read sibling `<pre>`.
- 6 language tabs: curl/Python/Node/PHP/Go/Java. Hero + animated terminal, marquee, SVG feature icons, live Playground tester, searchable endpoint table, auth format callouts, open-source banner, FAQ.

## README
- Updated: no env vars needed, 3 auth format table, examples use `you@gmail.com|abcd efgh ijkl mnop`, security section updated.

## Deploy + Live test (PROD, all pass)
- `node .../vercel/dist/vc.js deploy --prod --yes` → Ready in 14s, aliased to main URL.
- docs / 200 (74KB), health 200, info 200
- no auth → 401, bad auth → 401
- `Bearer zeronmodz@gmail.com|vqae nhux pqez izqz` → generate mixed/custom/batch 200, read 200 (2 msgs), count 200 (total 306/unread 240), unread 200
- format2 X-Gmail-Address + Bearer pass → 200

## Status
- FULLY DONE. Live: https://dev-zeron-temp-gmail-api-v1.vercel.app
- Optional next: GitHub public repo push so anyone can self-deploy.

# ------------------------------------------------------------
# Session 4: 2026-07-31 — docs website v3 (mobile-first animated redesign)

## Time: ~19:00-19:30
## User: Hasan (DevZeron)
## Phone: Phone 1 (Samsung A05m)

## Request
Website mobile friendly, all screen/device friendly, more modern, more animated, icons everywhere, banners, self-made images/illustrations, beautiful.

## What I Did (public/index.html full rewrite → v3)
- ui-ux-pro-max skill design system: Modern Dark (cinema), motion 8/10 complex, Inter+JetBrains Mono, accent green #22C55E / blue #38BDF8 / violet #A78BFA.
- Icon sprite system: 49 <symbol> ids, 43 used via <use href="#i-...">. All icons self-made inline SVG (Lucide-style strokes) — no emoji, no external images.
- Animations: scroll progress bar, ambient floating glow blobs (bg-glow), animated gradient hero title, pill entrance stagger, glassmorphism cards, animated mail-flow SVG diagram (How it works: alias → Gmail INBOX with dashed animated path + bobbing floating mails), hero animated counters, marquee with edge fade masks, stats band with animated counters, card hover lift + top gradient line + desktop tilt (pointer:fine & no reduced-motion only), FAQ accordion, spinner in playground status, btn shine sweep.
- Mobile-first: hamburger drawer nav + overlay (44px targets), responsive grid-2/3/4 auto-fit minmax(100%,...), fluid clamp() type, viewport-fit=cover + safe-area padding in footer/drawer, tables horizontally scrollable with sticky header, hero grid collapses to 1 column <980px, stats band 2-col <860px.
- Accessibility: aria-labels, focus-visible rings, aria-expanded on burger/FAQ, prefers-reduced-motion kills all animation, 4.5:1 text contrast kept.

## Verification
- HTMLParser: 0 errors, no unclosed tags (1583 tags, 107KB).
- Sprite check: 43 used / 49 defined, MISSING: none.
- Deployed PROD: docs / 200 (107KB), sprite/drawer/burger present in served HTML, API generate mixed still 200.

## Live URL
https://dev-zeron-temp-gmail-api-v1.vercel.app

## Next
- (optional) GitHub public repo push so others can self-deploy.

---

## Session 5 — Material Design 3 Rebuild (docs v4) — 01:40 AM
- User complaint: docs v3 mobile e "one side e dekhe, arek side empty" (two-col grid). Request: m3.material.io deke banano, joto shombhob toto Material component use kora.
- Research: m3.material.io fetch fails (JS-required) → used material-web.dev + jsdelivr flat listing + .d.ts API dumps. @material/web latest = **2.5.0**.
- Full component inventory verified: core (button, checkbox, chips, dialog, divider, fab, icon, iconbutton, list, menu, progress, radio, select, slider, switch, tabs, textfield) + labs (navigationdrawer/-modal, navigationbar/tab, card elevated/filled/outlined, badge).
- Key APIs learned (saved to learned/2026-07-31-material-web.md):
  - md-navigation-drawer / -modal: `opened`, `pivot` ('start'|'end')
  - md-navigation-bar: `activeIndex`; event `navigation-bar-activated` (`e.detail.activeIndex`)
  - md-navigation-tab: `label`, `badgeValue`, icon slot
  - md-tabs: `activeTabIndex`, event `change` (`e.target.activeTabIndex`); md-secondary-tab `data-lang`
  - md-menu: `anchor="id"`, `open`; md-menu-item `.value`
  - md-filter-chip: `.selected`; md-assist-chip / md-suggestion-chip: `label` + icon slot
  - md-dialog: `.open`, form method="dialog", md-text-button form+value
  - md-radio: `.checked` property (custom element `:checked` does NOT match — must find via JS)
  - md-slider: `input` event, `.value`
  - md-linear-progress: `.value`/max fraction; md-circular-progress: `indeterminate`
  - buttons support `href`/`target`; md-list-item `type="link"` `href`
- Rebuild: public/index.html fully rewritten (122KB, 1533 tags, HTMLParser 0 errors). Mobile-first single-column layout (fixes the "one side empty" bug). ~29 component types used:
  nav-drawer, nav-drawer-modal, nav-bar+tab, elevated/filled/outlined-card, badge, tabs, outlined-select, outlined-text-field, slider, checkbox, radio, switch, fab, dialog, menu+menu-item, chip-set, assist/filter/suggestion-chip, linear+circular-progress, icon, icon-button, list+list-item, divider.
  - Theming: material-color-utilities seed #22C55E → M3 dynamic tokens `--md-sys-color-*`, light/dark toggle + localStorage + meta theme-color. Import map esm.run pinned @material/web@2.5.0 (all CDN URLs 200 verified).
  - Old 49-icon SVG sprite fully removed (grep confirmed 0). Brand/social logos kept as inline SVG (not in Material Symbols).
  - Playground upgraded: slider (limit), checkbox (unread), radio (batch type), switch (pretty JSON + save creds localStorage), uid/tag field, password visibility toggle, circular progress loading, endpoint select w/ 14 options.
  - Endpoint filter: md-outlined-text-field search + md-filter-chip category chips (all/generate/read/manage/public).
  - Reference: md-tabs language switching + md-menu default-language picker (curl/Python/Node/PHP/Go/Java).
  - App Password help md-dialog + md-badge "v1" + bottom md-navigation-bar (mobile) + scrollspy activeIndex + back-to-top md-fab.
- Tests: tests/test_aliases.py 8/8 OK.
- Deployed live: `node .../vercel/dist/vc.js deploy --prod --yes` → Ready. Verified: `/` 200 (122101B), all md-* tags served, old sprite count 0, /api/info public 200, /api/generate/mixed with real creds → 200 alias JSON. All good.

## Live URL
https://dev-zeron-temp-gmail-api-v1.vercel.app
