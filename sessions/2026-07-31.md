# Session: 2026-07-31 — ZerFi Android App Build (v2 Fix)

## Time: 05:45-06:00
## User: Hasan (DevZeron)

## Problem
v1 app crashed on any click. 3 major issues:
1. BottomNavigationView tint used `@color/primary` (needs ColorStateList)
2. NavHostFragment missing `app:navGraph` + `app:defaultNavHost` 
3. Fragments had NPE from missing `isAdded()` checks

## Solution
- Created `res/color/bottom_nav_color.xml` selector
- Fixed activity_main.xml NavHostFragment setup
- Rewrote all fragments with proper lifecycle safety
- Changed from Python script execution → direct root shell commands (`iw`, `wpa_cli`)
- Removed Termux dependency — app now standalone

## New APK
- Location: /data/data/com.termux/files/home/ZerFi.apk
- Size: 15MB
- Build: `./gradlew assembleDebug --no-daemon`
