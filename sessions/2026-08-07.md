# Session 2026-08-07

## Device
Phone: (current) — request-free lat long not detected. Note: multi-phone rule in effect.

## Time: (start ~10:40Z) — Project: dev-zeron-temp-gmail-api-v2

User asked (Banglish): fix Temporary-Gmail-Vercel-Simple → Firebase as DB, verify Gmail+App Password are real, issue key, key-based API, security-first, redesign light theme per current/Instagram trends, deploy Vercel, credit Api_By/Tg_Channel on every response.

### What I did
- Read project `/storage/emulated/0/Download/Temporary-Gmail-Vercel-Simple/` (v1 open-source: Bearer email|apppass). Read profile + session + project memory first.
- Researched 2026 SaaS + Instagram design trends (product-led hero, bento grid, whitespace clarity, IG gradient #fcb045→#fd1d1d→#833ab4, hairlines, grotesk).
- New backend files: `api/firebase.py` (RTDB REST via urllib, uid=sha1hex(email)), `api/crypto.py` (Fernet password encryption, `key_` gen), refactored `api/index.py` (key-based auth, register/key/revoke, rate limit, security headers).
- Rebuilt `public/index.html` v5 — light theme Instagram-style, key playground, bento, animations.
- Updated requirements.txt (+cryptography), README.md (v2 flow + firebase rules), .env.example, firebase.rules.json.
- Tests: 18 pass (flaky `test_register_requires_firebase` transient once — passes 3x after).
- Set Vercel env (prod): FIREBASE_DATABASE_URL, FIREBASE_APP_ID, FIREBASE_API_KEY, API_KEY_SECRET.
- Deployed live: https://dev-zeron-temp-gmail-api-v1.vercel.app ✓ verified register/key/generate/count(340/248)/revoke + credit fields everywhere.

### Bugs hit & fixed
- 415 on `request.json` of form POST → `request.is_json` guard (form_value helper).
- Firebase "Invalid token in path" because `.`/email invalid in RTDB path → stored users by sha1hex(email) uid.
- Flask `both()` lost `/api` rules in rewrite → re-added.
- Frontend `buildPlaygroundUrl` built path but returned wrong var → fixed, and never `encodeURIComponent` the `@` in emails (path needs literal @).

### 🔴 SECURITY TODO (told user)
- Firebase RTDB rules open. Add `FIREBASE_DATABASE_SECRET` on Vercel + apply firebase.rules.json (deny public read/write to devzeron/keys_v1/**). README documents.
- Keept API_KEY_SECRET strong.

### Open items
- Optional: add FIREBASE_DATABASE_SECRET, close DB rules.
- Optional: multi-account (multiple keys per user) later.

Files changed (project dir): api/{index,firebase,crypto,aliases}.py, requirements.txt, README.md, .env.example, firebase.rules.json, public/index.html, tests/{test_crypto,test_register_flow}.py

## Round 2 (same day) — Mobile responsive fix + new features

User: "mobile screen e elomelo, kichu jinish theke jache, fix koro + more features"

### Fixed (mobile layout)
- **Root cause**: `.hero-grid` 2-col at all sizes → on mobile right card clipped/hidden. Now 1-col <=899px (also register-grid aligned).
- hero-cta full-width buttons on mobile; term/code `font-size:clamp(11px,3.4vw,13.5px)`; `pre{overflow-x:auto;max-width:100%}` so nothing clips; section padding reduced; safe-area-inset paddings for notched phones; mini-stat compact.
- Confirmed via live deploy: hero-grid fix, sys-pill, theme, toTop, safe-area all served.

### New features added
- **Light/Dark theme toggle** (topbar button, `data-theme` on <html>, persisted `dz_theme`, respects prefers-color-scheme default; no flash).
- **Live server-status pill** in topbar (fetch /api/health → online/degraded/offline, pulsing dot).
- **Back-to-top FAB** (gradient, shows after 480px scroll).
- **"How it works"** 3-step section (register → verified → call with key) with counter badges.
- **Playground**: "Remember key on this device" checkbox (localStorage, opt-in) + "Clear response" button.
- Nav now includes How it works.

### Validate
- HTMLParser clean (no unclosed), node --check JS OK, live deploy ready.
- Gotcha: theme init key vs toggle key mismatch (dz_theme) — fixed.
- Deploy cmd unchanged: `node .../vercel/dist/vc.js deploy --prod --yes`

## Round 3 (same day) — Menu size, custom scrollbar, polish, robustness

User: "uporer menu choto hoye ge, custom scrollbar banao, ar kichu improvements, design modify kor."

### Done (all live)
- **Bigger top nav**: desktop links 15.5px/padding 10px 17px; mobile drawer 16px; CTA + theme-btn + sys-pill bumps.
- **Custom scrollbar** (theme-aware): global thin gradient thumb (IG gradient) + hover; code blocks get gradient scrollbars; Firefox `scrollbar-width`/color.
- **Mobile hero centered**: badge/sub/CTAs/trust centered on mobile (per "sarai majhe dao").
- **Reveal SAFETY NET**: on window load a 1.2s fallback + visibilitychange reveal everything never-obscured-in-`in` → nothing "theke jay na".
- **Scrollspy**: active section highlighted in nav highlight.
- **Button shine** animation on primary CTA; hover lift on tiles/panels/specs.
- **Clickable endpoint rows** → auto-preload playground selector + tag + smooth-scroll + focus key.
- Gotcha: theme init/toggle key aligned to `dz_theme`.
- Page now 74.9KB; HTML/JS validated; live deploy ready; /api/health OK.

## Round 4 — Top-bar redesign + CTA banner + hero chips (17:26)
- User: fix top status bar (cleaner), redesign 3-line burger/menu, make "API online" pill prettier, add banners/images wherever suited.
- Header rewritten: `.top-ribbon` IG-gradient strip (5px), `.logo-txt` (DevZeron/Temp Gmail API stacked), `dev.`.top-right` group`, `sys-pill` now a `<button>` with `.dot` + ping animation (online green / offline red), `hide-sm` hides Get-free-key on mobile, new burger icon `M4 17.5h10`.
- Burger shows under 820px, drawer top:69px drops below ribbon-ish topbar; `.logo-txt small` hidden on mobile.
- CTA → `.banner-inner` gradient banner card (rounded 28px, floating blobs, key medallion, white text, glass/gradient CTAs).
- Hero terminal card got 3 Instagram-story chips (verified/key_issued/alias ready) with conic-gradient rings + floaty anim.
- All CSS vars confirmed; HTML/JS validated (HTMLParser OK, node --check OK); deployed live; /api/health OK.

## Round 5 — Zoom-lock + full docs + prompt + color/anim upgrades (17:35)
- Zoom in/out locked: viewport meta maximum-scale=1,user-scalable=no; html/body width:100%;max-width:100%;overflow-x:hidden; min-width:320px — nothing exceeds, layout stays centered at any zoom.
- FULL PER-ENDPOINT DOCS section (#docs): docs-nav sidebar + panel; every one of 17 endpoints × 5 languages (curl/Python/Node.js/PHP/JS) generated runnable snippets (BASE+samplePath), params table + response shape. 85 snippet combos tested ALL OK.
- Build-a-website PROMPT section (#buildsite): full copyable "Build a website with this API" prompt in a textarea (#promptBox) + copy button — dashboard onboarding, inbox, search, revoke, toasts, gradient UI spec.
- Color/motion: added --g4/--g5/--g6 + --grad-anim (animated), .grad-text gradient shift, aurora blobs behind hero, btn-live sheen on primary CTA, social-proof story-ring avatar bar, prefers-reduced-motion guard.
- Page 104K; HTML/JS validated; live deploy done; /api/health OK.

## Round 5 FIX — blank page (17:50)
- BUG: renderCode uschhiLANG[doclangs] -- always LANG undefined, top-level renderCode() throw korechhe, shob script band. Reveal safety net attach poche khuj arena → sob .reveal hidden → blank.
- FIX: LANG→DLANG. Vali el DLANG array tchhi (DLANG/DLNAME). Sob ♂runtime error thorun dissolved.
- Test: domstub + full script run = "SCRIPT COMPLETED OK". HTML/JS valid, redeploy live. Refresh karo spin.

## Round 6 — VS Code-style editor, overflow fixes, mobile menu, JSON highlight (18:20)
- User: make code sections look like a real code editor; fix glitches where tags/pills overflow cards; update mobile menu; more modern + features.
- CODE EDITOR makeover (docs .code-card → .editor-card): macOS traffic-light dots (red/amber/green), filename tab, syntax highlight (comments/strings/keywords/numbers) via new hlCode(lang,code) per-line tokenizer, line numbers gutter, code-line hover. Copy uses data-snippet (not textContent) so line numbers don't leak.
- Overflow fixes: .ep-row grid cols 64px|1fr|minmax|auto + media ≤1000px reflows (method/route top, desc+tag full-width, tag justify-start); tables/pills min-width:0, overflow-wrap:anywhere. No more tag escaping cards.
- Mobile menu (#burger): backdrop (.nav-backdrop) + slide-in animation (opacity/transform), body.menu-open locks scroll, closes on link click or backdrop tap.
- Hero terminal: dark editor bg (#161a22) + VS Code status bar (branch main* UTF-8 Bash Ln Col Spaces) + window dots.
- Playground preview: traffic-light dots + filename tab; added generic pre .kw/.st/.nm/.cm token styles so JSON highlight keeps working.
- Page 108K; HTML/JS validated + full-script smoke run OK; deployed live; /api/health OK.

## ~23:30Z — Project: terabox-downloader (Railway)
User: TeraBox69xBot railway toy; "TeraBox download failed" fix + admin edit to 5271912123.
- Deployed project, added BOT_TOKEN env (railway variables --set).
- Admin: replaced bot_data.json admin with 5271912123 (verified json).
- Root cause: old terabox.be API returned dead worker link -> download failed.
- Fix: mn-bots free worker API + retry/validation -> m3u8 -> ffmpeg. Deployed.
- caveat documented in projects/terabox-downloader.md (ephemeral fs, flaky free APIs).
