# Session — 2026-08-18

## 22:40 — dev.zeron.guest (Sketchware Pro #608) build kora hoyeche
- User: Sketchware Pro #608, package `dev.zeron.guest`, project empty (sudhu Material Colors import kora).
  Request: storage scan guest*.dat → M3 list, long-press copy/preview; 3 edit (UID/Pass/Path), inject replace, backup `.zeron`.
- Ami korechi:
  - Check korechi `.sketchware/data/608/` — ei project e java folder cilo na ar view/logic binary (empty).
  - Create korechi `/storage/emulated/0/.sketchware/data/608/files/java/GuestCore.java` + `GuestUi.java`.
  - Create korechi `/storage/emulated/0/.sketchware/data/608/files/resource/values/themes.xml` (Theme.ZeronGuest ← Theme.Material3.DayNight.NoActionBar).
  - Download korechi androidx jars (appcompat, core, material 1.14.0-alpha06, etc.) → compile test korechi → CLEAN.
  - JSON inject logic JVM e verified (uid/pass replace + extra keys preserved).
- Note: `com.google.android.material.R` use kori nai (sketchware merge risk) — attrs by name resolve kori.
- User setup needed: permissions add + onCreate More Block `GuestUi.setup(this); setContentView(GuestUi.build(this));` + build/sign (release_key.jks).
- Device: House phone (OPPO CPH2817) se asbe kivabe? User ekhane a6e — phone identify kaire nai, session e unresolved.
## 23:20 — ✅ REDESIGN COMPLETE → view-file driven M3 UI (GuestUi.java DELETED)
- Context: user return "What did we do so far?" → ami full state ta dekhi, then unprompted ei kisi komi:
  GuestUi.java (programmatic UI) delete korechi → native Sketchware `view` file e M3 design baniye ditam
  (fork-generated layout = reliable, version upgrade-safe, proper Theme/Resources integration).
- **The big discovery**: user actually tried building at 23:23 — debug.txt showed build reached aapt2
  link and FAILED only on my drawable bad hex `#33930000A` (bg_btn_errorcontainer) →
  proved: files/resource compiles, view-file→layout gen works, md_theme_* colors resolve.
  Fixed the hex to `#33BA1A1A`, scanned all drawables (all valid now).
- Note participant-compatible build: 604 calculator uses ONLY `addSourceDirectly` block in
  `@MainActivity.java_onCreate_initializeLogic` — copied that exact pattern for #608 logic file.
- Created: `swcrypt/gen_608_view.py` (44 comp M3 layout, schema mirrored from p4), `gen_608_logic.py`
  (wiring Java), encrypted both via Crypt → wrote to 608/view + 608/logic (round-trip verified,
  R.id references cross-checked vs view ids — NONE missing).
- GuestCore.java updated: scan=exact `guest100067.dat` only + renderResults + showPreviewDialog.
  Compile-clean (android-34 + material 1.14, exit 0). GuestUi.java file removed.
- Backups: 608/view.bak, 608/logic.bak. Memory git-pushed with full project doc.
- NEXT: user Build (signed) → verify runtime.
