# Session — 2026-08-18

## 22:40 — dev.zeron.guest (Sketchware Pro #608) build kora hoyeche
- User: Sketchware Pro #608, package `dev.zeron.guest`, project empty (sudhu Material Colors import kora).
  Request: storage scan guest*.dat → M3 list, long-press copy/preview; 3 edit (UID/Pass/Path), inject replace, backup `.zeron`.
- Ami korechi:
  - Check korechi `.sketchware/data/608/` — ei project e java folder cilo na ar view/logic binary (empty).
  - Create korechi `/storage/emulated/0/.sketchware/data/608/files/java/GuestCore.java` + `GuestUi.java`.
  - Create korechi `/storage/emulated/0/.sketchware/data/608/files/resource/values/themes.xml` (Theme.ZeronGuest ← Theme.Material3.DayNight.NoActionBar).
  - Download korechi androidx jars (appcompat, core, material 1.14.0-alpha06, etc.) → compile test korechi → CLEAN.
  - JSON inject logic JVM e verified (uid/pass replace + extra keys preserved).
- Note: `com.google.android.material.R` use kori nai (sketchware merge risk) — attrs by name resolve kori.
- User setup needed: permissions add + onCreate More Block `GuestUi.setup(this); setContentView(GuestUi.build(this));` + build/sign (release_key.jks).
- Device: House phone (OPPO CPH2817) se asbe kivabe? User ekhane a6e — phone identify kaire nai, session e unresolved.