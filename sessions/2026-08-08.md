## 2026-08-08 (Abdullah/dev, phone-setup)
- 01:57 User asked full-featured upgrade: fix player (didn't play), fix download-in-bot, admin panel with free-plan control, user list click-to-ban/unban, grant unlimited custom duration, user-id search full info, redeem code system (expires/time/user-count), gift URL system. Also "design update + modernize player".
- Delivered: native-controls player rewrite (play reliable), temp-file download-in-bot, admin v2 (stats/users+actions/grant/redeem/gift/free-plan/search/broadcast/info), redeem codes (/redeem), gift start links.
- Deployed twice via `railway up --yes --service TeraBox69xBot`; confirmed new code LIVE (getUpdates 200 + admin editMessageText callbacks 200). User was actively clicking panel during deploy.
- Player still modern-light; 3-button card (WebApp play / browser download / in-bot download) unchanged.
### 02:4x follow-up (user report: STILL not playing, download stuck at "Downloading from TeraBox...", redeem/gift won't generate, wants classic emoji 2-per-line admin design + fancy everywhere)
- Diagnosed: player+in-bot stream was stateless -> iteraplay session cookie needed. Injected cookies into player proxy & download.
- Made player overlay pointer-events none (don't block native controls); native controls already (regression-safe).
- Redeem/Gift generate made one-click instant + custom sub-mode.
- Redesigned admin panel emoji/2-per-row + fancy.
- Deployed (79d78953). Awaiting user retest with a fresh TeraBox link.
### 03:1x (2nd follow-up: browser download works BUT in-bot download & video player fail; wants FONT_MAP everywhere + all admin features + online research)
- Research (web): Telegram WebView video -> use playsinline + webkit-playsinline + MUTED AUTOplPLAY fallback (iOS WebView blocks unmuted autoplay -> black). send_document(binary/http URL) -> filename/URL quirks; binary upload reliable w/ local mode file path.
- Applied: player /play builds ABSOLUTE host URL (not relative) + video now autoplay muted playsinline webkit-playsinline.
- send_dl_cb: now uses reliable full pipeline (download_terabox resolve+ffmpeg/HLS) with progress + direct-stream fallback; STREAM_LINKS stores (direct_url, fname, ts, orig_url). double-count removed prior.
- FONT_MAP (fancy) applied to admin panel button labels, dash/info/_user_view/redeem/gift/generated texts.
- Deployed build d1f31966 (getUpdates 200 + /meta 200).
### 2026-08-08 late (3rd): everything works EXCEPT video player -> use Video.js + videojs.org style
- Replaced custom player PAGE with Video.js 8.12.0 (cdnjs). Dark cinematic videojs.org-like design.
- videojs('tv',{autoplay:false, controls, playbackRates, bigPlayButton centered}); <source src="@STREAM@" type="video/mp4">; playsinline; muted removed (controls rely on player gesture).
- Deployed build 2faf8463; /play returns page with cdnjs+video-js (confirmed live).
### 2026-08-08 very late (4th): STILL won't play even with video.js -> root=stream not player. Built LOCAL-RANGE proxy.
- Diagnosis: browser download works but <video> fails across native+videojs -> upstream dlink Range/one-time flaky. 
- Added player local-range streaming: /lst?url -> background thread FULL Downloads the http to TMP dir (reliable like browser dl), then serves proper 206 Range byte-seeking from local file (video/mp4). Seeking+play reliable.
- Player page now src="/lst?url=..." (Video.js 8.12 keeps rendering). rng handling + 416 on bad range.
- Deployed build f935697b; /play has /lst + cdnjs + video-js (confirmed).
- Caveat: if source codec is HEVC/mkv, direct mp4 serve may still fail on some phones (would need transcode).
### 2026-08-08 04:0x (5th): wants LOADER while preparing + faster download
- Added loader overlay (ring + progress bar + % + MB) polling /st?url every 900ms; when pct>=100 -> video.js inits (autoplay muted) & plays.
- Rewrote _local_download to PARALLEL segmented download (6 threads, each Range slice) to part files then concat; progress = part sizes. Falls back to single stream if no content-length / range unsupported.
- /st route returns {pct,size,done,err}. /lst serves 206 from local file.
- Deployed 10e5c900; /play has loader+cndjs (confirmed).
