# Session — 2026-08-10 (DevZeron / Hasan)

## Device: Termux (Android) — current phone e kaj. Device file: check devices/.

## 01:40 — OPENCODE UPGRADE 1.18.5 → 1.18.15
User asked: opencode update ase kina dekhe update koro.

### Ki korlam
1. `opencode --version` = 1.18.5. npm latest = 1.18.15 (opencode-ai pkg) → update ase.
2. `opencode upgrade --print-logs` → 180s hang (koi output nai). Self-updater Termux glibc install e atke.
3. Network fine; note: **opencode repo migrate holo sst/opencode → anomalyco/opencode** (api.github 301).
4. Latest release v1.18.15 assets: CLI Linux arm64 glibc build = `opencode-linux-arm64.tar.gz`.
5. Install layout: `/usr/bin/opencode` = 7KB ELF wrapper (Termux glibc interop), real binary `/data/data/com.termux/files/usr/share/opencode/opencode.real` (glibc, interpreter /lib/ld-linux-aarch64.so.1).
6. Manual upgrade: backup → download v1.18.15 tar.gz (60MB) → extract → replace opencode.real → chmod → `opencode --version` = **1.18.15** ✅ → backup + tmp cleanup.

### Shikho
- Raw glibc opencode binary run korle `zsh: no such file or directory` (ENOENT) — interpreter `/lib/...` nai Termux e. ONLY wrapper diye chole. Swap korle `opencode.real` name e rakhte hobe (wrapper path hardcoded).
- `opencode upgrade` self-updater Termux glibc build e hang kore → manual download+replace safest.
- Release binary tar.gz = glibc (not musl) for Termux. Musl asset alada (`opencode-linux-arm64-musl.tar.gz`).
- Do NOT `pkill -f "opencode upgrade"` — full cmdline match self-shell kill kore. Use pgrep or exact process pattern.

### Files changed
- Replaced `/data/data/com.termux/files/usr/share/opencode/opencode.real` (178MB → 183MB, v1.18.15)
- Cleaned /data/data/com.termux/files/usr/tmp/opencode/ temp + backup

## 17:15 — TEMP-MAIL TEMPLATE → UNIVERSAL HOST SUPPORT (v2.1)
User: "সবাই vercel e host korbe na — sob platform e supported koro, secret thakbe na, sob edit kore nibe."

### Ki korlam (zeron-temp-mail-brutal-template)
1. `proxy.js` — universal proxy CORE (env-only, zero-dep, Node>=14). Export run/parseBody/expressHandler/eventHandler. **jest require("./config.json") core e NAI** — esbuild bundler (Vercel/Netlify/CF) missing-file require e ভয়। config.json only server.js e fs-based load hocche.
2. `api/index.js` → Vercel: thin re-export.
3. `server.js` → Node standalone (static + POST /api). PORT. config.json boot-load. Sensitive file 404.
4. `netlify/functions/api.js` + `_redirects` + `netlify.toml` (esbuild bundler) → Netlify /api.
5. `functions/api.js` → Cloudflare Pages, SELF-CONTAINED (workerd: no require core / context.env).
6. `api.php` → cPanel/shared host (curl + allow_url_fopen fallback). **PHP8.5 curl_close deprecation fix** (PHP_VERSION_ID guard).
7. `package.json`, `Dockerfile`, `config.example.json`, `.env.example`, `.gitignore` update.
8. app.js proxy auto-detect chain: `/api` → `/api.php` → direct (standalone key). 404/405/501/non-JSON => "noproxy".
9. README full deploy matrix + one-click buttons (Vercel/Netlify/Render/Railway).

### Test results (sab verified)
- node --check all OK; server.js health+generate 200; /server.js,/config.json 404 leak-proof; no-key 500 msg; OPTIONS 204.
- config.json fallback works without env var.
- api.php (PHP 8.5): health + readby raw @ 200. no-key → 500.
- python http.server static sim: /api → 501 (chain handles).

### Notes
- Env key: key_EnsATw-... (server-side only). GitHub push hoyni — user boleche push yes/no ask korte hobe (na bole gele push kori nai). Vercel live prothom probe korte hobe.
