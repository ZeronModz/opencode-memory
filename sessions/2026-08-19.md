
# Session — 2026-08-19

## 13:45 — MainBinding blocker → custom MainActivity.java override (FIXED)
- Context: #608 build note (13:49 in debug.txt, "Failed to compile Java files"): generated MainActivity.java
  used `MainBinding` (enable_viewbinding true) but class nai → viewbinding generation broken in this fork.
- **KEY INSIGHT**: same-fork projects **4 & 603** (works) have custom `files/java/MainActivity.java` that
  OVERRIDES the generated one, and their `logic` decrypts to **0 bytes (empty)**. p4 logic file on disk = 16 bytes.
  → The fix pattern: custom MainActivity.java (setContentView + findViewById, NO ViewBinding) + EMPTY logic.
- Applied to 608:
  1. `cp /storage/emulated/0/.sketchware/data/4/logic /storage/emulated/0/.sketchware/data/608/logic`
     (verified md5 identical = 88da45dac0127f2b509187432a3b8f7b; decrypts to 0 bytes). NOTE: first cp
     seemed not to propagate (later stat showed 7040); forced re-cp fixed it.
  2. Wrote `/storage/emulated/0/.sketchware/data/608/files/java/MainActivity.java` (package dev.zeron.guest,
     extends AppCompatActivity): setContentView(R.layout.main), DynamicColors, statusbar/navbar 0xFFF9FAEF
     + LIGHT_STATUS_BAR, findViewById all 16 ids, refreshPermStatus (onCreate+onResume), auto notif perm
     request on 33+, btn_grant/btn_notif/btn_scan/btn_inject wiring (bg threads, main.post updates).
  3. Kept GuestCore.java + files/resource/layout/main.xml override + view file (untouched).
- **Compile verification**: downloaded REAL androidx jars from dl.google.com maven into
  `/data/data/com.termux/files/usr/tmp/opencode/check/` (appcompat 1.7.0, core 1.13.1, fragment 1.8.3,
  drawerlayout 1.2.0, customview 1.2.0, activity 1.9.2, savedstate 1.2.1, lifecycle-runtime 2.8.6 .jar,
  lifecycle-viewmodel .jar, cardview 1.0.0) + fork's material classes.jar + android-34. Stubbed only
  compile-time-only classes (androidx.annotation.NonNull, LifecycleOwner, ViewModelStoreOwner,
  HasDefaultViewModelProviderFactory, fake R). **javac EXIT 0 — both MainActivity + GuestCore clean**
  (only deprecation note).
- Learned: new androidx lifecycle/jar packaging splits .class files (lifecycle-runtime.aar = empty;
  KMP jars lack root classes — MVP metadata only). AndroidX annotation 1.8.1 jar = 8KB w/o NonNull.

## NEXT
- User Build (signed) → expect success (M3 layout inflate + scan/inject runtime verify).
- If duplicate class or MainBinding still creeps in → also set enable_viewbinding:false in project_config.

## 14:10 — UI v2: scroll fix + auto-scan + scan notif + premium M3 redesign (user feedback batch)
- User (Bangla) 5 ta issue: (1) scroll na (2) perm paile card_perm hide (3) app open korle auto-scan + scan btn (4) scan er somoy notif alert + notif perm er kaj (5) latest M3 widgets premium design, "tumi tomer moton kore improve koro".
- Applied: main.xml rebuild (FrameLayout root → ScrollView → content + ExtendedFAB RESCAN), card_hero gradient FrameLayout, card_perm id + GONE on grant, MaterialButton+icons (7 vector ic_*), FilledBox TextInputLayout w/ startIcons, LinearProgressIndicator scan_progress, hero_chip status. GuestCore: scan notifications (channel + start/done). MainActivity: auto-scan on open (once on onResume), fab_rescan wiring, card_perm hide, notif triggers.
- Verified: javac clean (real androidx, coordinator added), ids/drawables/colors all matched, XML well-formed. aapt2 can't run on Android (host binary).
- Backup: pre-v2 main.xml kept as layout/main.xml (overwritten by Write) — old content preserved in project doc + git memory.
- Device: House phone (OPPO CPH2817) — still unidentified in session, note kept.

## 14:40 — UI v3: pinned hero + auto-fill/persist path + gap fix + credits + M3 everywhere
- User (Bangla): scroll hero bade, scan pore path auto-fill et_path, path SharedPreferences kore save, results row gap, colo rs sob values theke, TextInput model variety, anek M3 widget/animation, credit card + Telegram/GitHub links, aro features.
- Applied: pinned hero (LinearLayout root, hero fixed + scroll weight), auto-fill first result + tap fill via renderResults(pathField,onFill), SharedPreferences guest_inject (uid/pass/path/auto_scan) TextWatcher auto-save, renderResults premium rows (oval iconBox, gap 12dp, tag pill, margin 10dp, staggered fade animation), ALL hardcoded rgb → R.color.md_theme_* (+values tokens m3_hero_grad_*, m3_onPrimary_sub, m3_hero_icon_bg), MaterialSwitch auto-scan, Assist Chip status, TextInputLayout FilledBox+OutlinedBox+clear_text+helper, btn_paste clipboard, About card (DZ avatar, Made by DevZeron, Telegram/GitHub buttons → t.me/ZeronModz + github.com/ZeronModz, v1.3).
- New drawables: ic_copy/ic_paste/ic_telegram/ic_github, bg_avatar. Fixed til_pass startIconDrawable color→drawable bug.
- Verified: javac EXIT 0, ids/drawables/colors matched, XML well-formed. git+pushed memory.
- NEXT: user Build (signed) → runtime check (pinned hero, auto-scan, path persist, gap, credits links).

## 15:20 — UI v4: theme color ROOT fix + accurate icons + pretty JSON preview
- User (Bangla): edit text design hizibiji → sob ekei design + space; card_hero khub kharap, dui side e lege ache; button/switch color theme er sathe match na; icons broken/incomplete/bhul; long-press dialog theme color na + data format sundor JSON code style; aro feature.
- ROOT FIND: values-night/AppTheme existed (full green md_theme mapping) but **light mode e AppTheme nai** → framework default colorPrimary #AB6543 (brown) sob theme widget control korechhilo. Fix: LIGHT AppTheme (DayNight parent, full mapping) in values/themes.xml + brown namespaced colors green (colorPrimary/Accent/Dark/etc).
- Icons: ic_copy/ic_paste corrected (content_copy/content_paste real paths), ic_telegram/ic_github verified accurate; all single-path #FFFFFF.
- Uniform FilledBox fields (uid/pass/path, 18dp corner, 14dp gaps), hero margin 16dp + icon 56dp, fab iconTint.
- Preview dialog: pretty JSON (toString(4), JVM-verified) + colorizeJson syntax colors from theme tokens (primary keys/tertiary strings/error numbers/outline punct), monospace surfaceContainerLow box, COPY JSON/COPY PATH/CLOSE. Haptic CLOCK_TICK on row click. JSONArray import add.
- Verified: javac EXIT 0, XML clean, ids/drawables/colors matched, org.json format check OK. git pushed.
- NEXT: user Build (signed) → runtime theke check (theme color sob match, switch green, icons thik, preview pretty).
## 15:17–15:25 — Project 608 v5 BATCH INJECTION implemented
User: "Continue if you have next steps..." → I implemented the v5 batch feature:
1. New icons ic_check/ic_close/ic_upload (drawable/)
2. main.xml: hero_chips id, btn_inject solid tertiary (#386663), new Batch Inject card (et_import_path, btn_select, btn_load, batch_status, btn_done, btn_cancel)
3. MainActivity v5: batch engine (importAccounts/startBatch/loadAccount/finishBatch), SAF picker ACTION_OPEN_DOCUMENT REQ_PICK=7001 + onActivityResult/readUri/queryName, btnLoad content:// or path, btnDone→saveAccountToZeronGuest→next, btnCancel→finish; hero collapse scroll listener
4. Compile EXIT 0 (added RestrictTo$Scope stub + updated fake R with new ids/drawables)
5. ParseTest JVM verified: 4 cases pass. Learned: android.jar org.json = stub → json.jar must precede android.jar in classpath.
All ids/drawables/colors matched, XML well-formed. Memory updated + git push pending.
Device: current phone (Termux, project 608 files on /storage/emulated/0/.sketchware).

## 15:25–15:45 — Project 608 UI v6 ADVANCED upgrade
User (Banglish): hero scroll effect sundor na → unique advanced; batch session jeno app background/kill/1 maash poreo resume hoy (koto done/koto baki); anek advanced + new Material features add.
Done: (1) bg_hero_curve wave-gradient hero + parallax collapse (icon tilt/scale, title/sub slide+fade, chips slide) + FAB hide/show on scroll direction (2) batch session persistence via prefs (batch_active/accounts/index) + restoreBatch on create + accountsToJson round-trip verified (3) scan depth toggle MaterialButtonToggleGroup QUICK6/DEEP12 persisted (4) FAB spin ObjectAnimator while scanning + stopSpin onPause/onDestroy (5) M3 Snackbar (ANIMATION_MODE_SLIDE) for batch events (6) TooltipCompat (appcompat.widget — core.view e nai) tooltips (7) batch_progress LinearProgressIndicator determinate. Compile EXIT 0, XML all well-formed, ids/drawables/colors matched, PersistTest round-trip OK. Memory pushed.
Device: current phone (Termux, project 608). User in-app Build pending.

## 15:50 — Build error fix: Widget.Material3.Button.ToggleButton not found
User build failed (aapt2 link): ToggleButton style nai fork er material e. Fix: OutlinedButton style + checkedBackgroundColor attr remove (o exist na). Verified all styles/attrs against material-1.13.0 values.xml. XML OK, javac EXIT 0. Problem logged (problems/2026-08-19-togglebutton-style.md). Memory push.
Device: current phone (Termux, project 608).

## 16:10–16:30 — Project 608 UI v7: real slide-under hero + logo + credits + typography + batch persistence fixes
User feedback: wave hero kharap; batch path+data persist na + ultapalta; design premium/unique/latest M3; credits card modern; alada logo; typography.
Done: root→FrameLayout, hero=overlay (content now real under-side slides, paddingTop 150dp), bg_hero_round, brand logo_gi (rounded gradient + person) hero+credits, credits bg_credits gradient + divider + colored social buttons + v1.4 pill, typography kickers (PERMISSIONS/STORAGE/SINGLE INJECT/MULTI ACCOUNT), batch persistence fix: K_IMPORT_PATH saved+restored, KB_CUR_UID/PASS saved (edited field values return), etPath overwrite guard when batchActive. Compile EXIT 0, ParseTest+PersistTest pass, XML all matched. Memory push attempt.
Note: last git push (ToggleButton fix) FAILED (network) — commit local. Try ing push again now.
Device: current phone (Termux, project 608).

## ~17:00–17:20 — Project 608 v7.1: translucent hero, SKIP button, auto-skip saved, scan-depth fix, logo v2
User feedback: (1) hero scroll korle content upor diye gele hero halka transparent/blur-like hobe (2) batch e SKIP option lagbe (CANCEL e sob cancel hoy) — skip korle porer account e jabe (3) already saved account check koro — ZeronGuest.json e thakle pura skip (4) logo "bhange" — redesign (5) scan depth button e 2 dike "|" daga — remove (6) app background korle/kill korle count/data problem.
Done:
- logo_gi.xml v2: solid #4C662B rounded-24r square + WHITE Material `key` glyph (group translate 36,36 scale 2) — aapt gradient/aha person bad dewa (kryptic render na).
- ic_skip.xml: Material skip_next (white -> tinted primary at runtime).
- hero_overlay View (bg #52FFFFFF 32% white) inside card_hero, BEHIND content, alpha=fades to 1 on scroll (between bg and content → light translucent look, blur-ish without real blur).
- Scan depth QUICK/DEEP style → Widget.Material3.Button (filled, no stroke) — "||" daga gone (was OutlinedButton double-stroke).
- Batch: btn_skip (SKIP, OutlinedButton primary) + btn_done side-by-side row in batch card; CANCEL text button nichei.
- GuestCore.loadZeronGuestUids(): reads ZeronGuest/ZeronGuest.json → Set of saved uids (String.valueOf normalized + trimmed).
- MainActivity.loadAccount(): while-loop auto-skip accounts already in ZeronGuest.json (count printed), else load; status now "Done X · Remain Y · now A/N (already-saved auto-skip)"; finishBatch message differentiates all-already-saved.
- Persist hardening: saver TextWatcher now also persistBatch() on every keystroke (batchActive hole); onPause→persistBatch() → edited field values (KB_CUR_UID/PASS) + index + accounts je kono kill e survive kore.
Verify: javac EXIT 0 (full classpath: out:json.jar:material.jar:exts:android.jar; android.jar LAST, json.jar FIRST for org.json), ParseTest 5 cases pass, PersistTest roundtrip pass, XML well-formed x3. Runtime CP for tests needs material.jar + _ext jars + android.jar.

## ~17:40–18:00 — Project 608 v7.2: hero full-screen bug fix + M3 app bar + copy buttons + saved counter
User: hero now WHOLE SCREEN (kisu dekhai na); design same thakbe, scroll: text review color transparent/blur hoye pichonear jinis dekhai; more useful features; Material Design app bar; simple minimal animations.
ROOT CAUSE: hero_overlay View (match_parent height) inside FrameLayout — FrameLayout gives MATCH_PARENT child EXACTLY(full avail height) → hero forced full screen. FIX: overlay deleted; translucent-glass effect now via heroBg.setAlpha(): card_hero.getBackground() GradientDrawable tinted 255→(255*(1-0.62f)) on scroll (content behind shows through, "glass" look, no layout change).
Added:
- MaterialToolbar (#toolbar) TOP appbar, translationY -120dp hidden, reveals when scrollY>=60 (animate translateY0+alpha1), hides on scroll up. Contains tbtn_rescan (RESCAN) as end action. Last child of root = on top of hero.
- COPY UID / COPY PASS outlined buttons in INJECT card (clipboard, toast).
- saved_count line on batch card "Saved in ZeronGuest: N" + COPY PATH button; refresh after DONE/loadAccount; path = ZeronGuest/ZeronGuest.json.
- Toolbar reveal (~g = scrollY/60) + hero bg alpha fade — simple minimal animations only.
Verify: javac clean, ParseTest 5 cases + PersistTest pass, XML well-formed. R stub synced (card_hero/toolbar/tbtn_rescan/btn_copy_*/saved_count).
Next runtime check: hero NOT full screen; hero fades to glass while scrolling; toolbar slides in; copy buttons work.

## ~18:30–19:10 — Project 608 v7.3: NPE fix + appbar remove + scan-depth round + DONE align + batch name + MAP buttons
CRASH: NPE at MainActivity.restoreBatch:637 — startBatch(false)→loadAccount() auto-skip loop found ALL remaining already-saved → finishBatch() nulled accounts → restoreBatch then did accounts.size(). FIX: after startBatch(false) in restoreBatch, guard `if (accounts == null) return;`; also startBatch snapshots `n` BEFORE loadAccount for announce snackbar.
User fixes:
- Appbar: FULLY REMOVED (toolbar + tbtn_rescan + code + imports) — scroll korle ar notun jinish ashe na; hero=only overlay.
- card_hero: transparent effect BAND — heroBg.setAlpha removed; hero solid color thakbe. Parallax (icon/title/sub/chips) rakhchi.
- Scan depth: OutlinedButton style + strokeWidth=0dp + height 40dp + cornerRadius 20 → rounded pills, "||" daga gone, round shape thakbe (filled na).
- DONE&SAVE: explicit height 52dp both buttons + insetTop/Bottom 0 + singleLine + maxLines 1 + iconSize 18dp, text "DONE&SAVE" (hashtag-free single line) → SKIP er sathe perfectly aligned (weight 1 each).
- Batch name: GuestCore.Account.name (+NAME_KEYS name/user_name/username/account_name/nickname/Name/USERNAME/nick_name/display_name) parsed + persisted (accountsToJson adds name); batch_name pill TextView shows current account name (bg_pill_ghost 14dp rounded, translucent #14FFFFFF + stroke #52394E1F).
- 3 MAP buttons under INJECT NOW: MAP 1/2/3 (TonalButton tonal colors, ic_map pin icon) → open https://c.freefiremobile.com/?m=... in com.dts.freefireth (setPackage), fallback browser. Urls: 111A1A088B1DEEEF5954088D727E39B1BAE86K144 / 111A162C86FFE566A9447CE413FDE93BFEE2EK515 / 111A1173049BE93348A46505D2B97AD273846K194.
R stub fully rewritten clean (drawable ic_map+bg_pill_ghost, ids btn_map1/2/3+batch_name; toolbar/tbtn_rescan/hero_overlay removed).
Verify: javac clean, ParseTest 5 OK, PersistTest roundtrip OK (name="" preserved), NameTest (name= Apex Gamer / Killer_99 / empty) OK, XML well-formed. Grep clean (no leftover toolbar/heroBg refs).

## ~19:30–20:00 — Project 608 v7.4: DONE crash fix (accounts null) + scan-depth toggle back
CRASH: MainActivity.java:395 NPE — btnDone last-account flow: finishBatch() nulls accounts, then snack string built with accounts.size(). ROOT: auto-skip made it worse (loadAccount could null accounts mid-flow too). FIX: capture `int total = accounts.size()` BEFORE finishBatch; snack uses total; else-branch snack guarded by `if (accounts != null)` after loadAccount().
Scan depth: user chaitto "toggle button er moto" (bordered), strokeWidth=0 chole giechilo. REDO: MaterialButtonToggleGroup wrapper BAD/ber kora holo → plain LinearLayout + 2x OutlinedButton (checkable=true, cornerRadius 20, height 40dp, 4dp gap → no || double-line, borders visible = toggle look). Manual selection: selectDepth(int) sets scanDepth + prefs K_DEPTH + btnQuick.setChecked(depth<=6)/btnDeep.setChecked(>6); listeners on each. Removed btnGroupDepth field/findView/listener (id btn_group_depth kept on LinearLayout, harmless).
Verify: javac clean, ParseTest 5 OK, PersistTest roundtrip OK, XML OK. accounts.size() call-sites audited (all null-guarded now).

## 04:30 TeraBox VIP Player (y.html) — Ultimate Edition + Localhost hosting
**Device:** Hasan (DevZeron) / Android Termux / phone Home dir

**Kon kaj holo:**
1. `y.html` (1796 lines, /storage/emulated/0/Download/Telegram/) design overhaul complete:
   - 5 unique ornate profile frames (Genshin-style) — Bronze/Silver/Gold/Platinum/Diamond, sob avatar site e
   - First-time-only welcome bonus (+20 XP) — `bonusClaimed` field, user doc e save
   - Daily login bonus (+25 XP) — `lastDailyClaim` field
   - Top creators leaderboard (feed), frame collection showcase (profile, tap = preview)
   - Level-up celebration + confetti + frame unlock preview
   - Ambient aurora blobs, button shine, nav bounce, progress shimmer, card hover
2. **Admin localhost access fix:** Telegram er baire `?user=8708940588&name=DevZeron` query param → admin panel visible. Telegram e ignore (secure).
3. **Hosting:** `python3 -m http.server 8000 --bind 0.0.0.0` (workdir = Telegram folder) — LIVE HTTP 200 localhost + LAN 192.168.0.100
4. `serve.sh` script banano (bash diye chole — FAT fs te chmod +x kaj kore na)

**URLs:**
- Original: http://localhost:8000/y.html
- Admin: http://localhost:8000/y.html?user=8708940588&name=DevZeron
- LAN Orig: http://192.168.0.100:8000/y.html
- LAN Adm: http://192.168.0.100:8000/y.html?user=8708940588&name=DevZeron

**Vul fix:** Server aage home dir theke start hoyechilo (404) → PID kill kore Telegram folder theke restart ✓


## 04:45 OTHER-PHONE "Loading stuck" fix (y.html)
**Bug:** parallel phone browser e `?user` param chara khulle `userId=""` → `doc(db,"users","")` InvalidArgumentError → onload dead → "Loading..." + spinner stuck forever.
**Fixes (3):**
1. Guest fallback: no Telegram + no param → `userId="guest_"+Date.now().toString(36)`, `userName="Guest Player"` (empty doc id prevented)
2. `const tg = window.Telegram?.WebApp || { initDataUnsafe: {} }` + `tg.expand?.()` → Telegram script block hole crash nai
3. `shareVideo`: `tg.openTelegramLink` na thakle `window.open` fallback
**Verify:** node --check OK, server 200. Network issue thakle (router isolation) oi URL + hotspot test.

## 05:20 TeraBox VIP v3 — SMOOTH performance + frames redesign + features + PUBLIC tunnel LIVE
**User feedback:** "Ember Circuit / Starlight Arc valo; baki frame broken (name bhange/overlap), lag, ar o besi features chai"
**Fixed:**
1. **Frames redesign (v3):** sob ornament ekhn avatar er VITORRE — em-based (container font-size == size px → auto-scale). Side-wings/oversize remove korechi (name overlap hoy na). Royal Sunburst (gold, crown+burst), Frost Galaxy (platinum, dotted halo), Divine Aurora (diamond, crown+aura) — static conic gradient ring + 1 slow orbit + gem twinkle. `--ga`/spinRing (rotating conic = paint-heavy) FULL remove.
2. **Lag fix:** bg-blobs: `filter:blur(72px)` removed → radial-gradient glow + transform-only; btnShine: `left` layout anim → transform; confetti 38→22; per-frame animation count max 2 (transform/opacity = compositor); box-shadow pulse animations remove; `will-change` feed avatars remove.
3. **New features:**
   - 🔥 **Daily streak:** consecutive day bonus, +5 XP/day up to +25 extra; claim shows next bonus.
   - 🎡 **Spin & Win:** pietro wheel (8 slices conic), day-1 free spin, rewards 10–65 XP, firestore `lastSpin`.
   - 🏆 **Achievements:** 6 badges grid (shares/likes/follows/xp milestones), one-time +XP claim, `claimedAch` array.
4. **PUBLIC TUNNEL LIVE (Bangladesh→Saudi friend):** Pinggy finally connected (har matbar pinggy ISP-blocked hoye lage — ebar asole connect). PUBLIC URL now: `https://tbnpd-103-114-39-157.free.pinggy.net/y.html` + backup `.run.pinggy-free.link` (expire 60 min). tunnel.sh script: `bash /storage/emulated/0/Download/Telegram/tunnel.sh`
**Lesson:** bash tool persistent session e live-ssh bg job pipe block kore — tunnel chalaite `setsid bash -c '...' </dev/null >log 2>&1` detach pattern lagbena; AND `pkill -f 'ssh '` nijer shell-e kill kore (self-match) — `a[.]pinggy` bracket trick use koro.
**Verify:** node --check OK; served HTTP 200 (public tunnel-o 200); WHEEL 8 slices == WHEEL_REWARDS 8 entry (mismatch thakte thakte fix).

## 05:50 TeraBox v4 — LAG ki korlam (roots fixed) + spin v2 + booster + spree
**User:** "ektu-o smooth na — full lag; frames sundor koro; spin update koro; aro features, aro XP — kintu kono lag nai"
**Lag ROOT FIXES:**
1. **backdrop-filter: blur(20/25/15/10px)** sob remove (header, glass-card, feed-card, bottom-nav, modal) — eta chilo SOBcheye boro jank source (scroll=ongoing blur repaint). solid bg diye replace.
2. **Feed = incremental re-render** (`listenGlobalVideos` via `window.__feedSigMap`): purono `list.innerHTML = cardsHtml` FULL rebuild remove korechi → iframe RELOAD hote thakto (video restart) + full layout. Ekhon: sig (uploader/likes/timestamp) change hotei shudu oi card update. Unchanged card re-parent only.
3. **Orbit/twinkle gate `orb-mini`** (size≥44): feed/header 35-40px avatar → FULLY static; shudu profile(92)/showcase(58-118) animate. buildAvatar/buildProfileAvatar te class add.
4. **logoGlow filter animation** remove (paint heavy); base .fr.gem/.fr.spkA/.crown animation remove (gate-only). Confetti 22. Blobs blur→radial (aagei).
**Frames beauty:** static inset edge-light `::after` (inset 2px box-shadow) — premium look, zero anim. All frames update.
**Spin v2 (SPIN_SLICES 8):** emoji labels rotate-positioned, janky? nah — static conic + labels; rewards 10-65 + **⚡ ২x Booster ২৪ঘ** slice; spin stats (spinTotal/spinsCount) chip e; jackpot 65 → confetti.
**Booster:** `boosterUntil` firestore; addXP ×2 when active; chips e countdown dekhay.
**Share spree:** har 5th share +15 XP (`sharesCount % 5`).
**Verify:** node --check OK; backdrop-filter 0; public pinggy + local = HTTP 200. `rm feedFirstRender/WHEEL_REWARDS` dead refs.
**Tunnel:** 60-min pinggy active — ekhono tbnpd-103-114-39-157.free.pinggy.net live (# older URL).

## v5 — 8 Levels + Frame Selection + Admin Frame + Verified Seal
- RANKS extended 5 -> 8: Lv6 Mythic (10000/4x, chess-queen, #b388ff), Lv7 Legend (25000/5x, fire-flame-curved, #ff2a5f), Lv8 Celestial (60000/6x, fa-sun, #fff, maxXp 150000 'সর্বোচ্চ ডিভাইন'). All prev nextText updated.
- PERK_CATALOG +3 (lvl 6/7/8 perk rows).
- FRAMES 6-8: Void Rune / Royal Flame / Prism God; SPECIAL_FRAMES.admin = 'DevZeron Officer' (frame-admin). FRAME_DECOR extended.
- CSS: generic .fr.crown (static conic) shared; diamond crown = aurora gradient override; 3 new frame classes (conic bg only, static) + .frame-admin gold/black; .rank-elite-name static glow; .u-verified.vb = inline SVG seal (vbGrad gradient id, crown-ish polygon + check), .vb-lg >= lvl6.
- SELECTION SYSTEM: selectedFrame field in users doc (fetchUserData default null + setDoc on new); getActiveFrame(xp, forceLv) = forceLv > admin > selectedFrame > rankFrame; buildAvatar/buildProfileAvatar +forceLv param; selectThisFrame(lv) (rank-guarded), resetFrameSelection(); showcases tap-to-preview -> select button in Swal; .frame-preview.applied class; reset button row; admin gets extra officer card (previewSpecialFrame). renderUserName now renders verifiedSeal SVG + elite/diamond name classes.
- Rmoved badgePulse shake anim (lag rule); removed '-' unused.
- NOTE: preview/showcase/levelup pass forceLv so selected frame never leaks into them.
- Verified: node --check SYNTAX OK; local+LAN+pinggy HTTP 200 all green. Pinggy same URL still live (tbnpd...).

## v5.1 — bugfixes + 10 levels + extra frames + spin v5 (SFX) + features
- FIX: stray '}' after previewSpecialFrame removed (mixed with old fn closer).
- FIX: verifiedSeal duplicate SVG gradient id 'vbGrad' (all badges used 1st color) -> unique per-instance id vb_gr_...; redesigned glossy seal (scalloped gradient fill + ring + check + shine arc), elite(6+)/admin -> gold bigger (vb-lg).
- FIX: admin-cards .replace('profile-avatar-vip','avatar-frame') hack removed -> buildAvatar direct.
- RANKS 8->10: Lv7 maxXp 59999; Lv8 maxXp 119999 next 'Ascendant'; +Lv9 Ascendant Sovereign (120000/7.0x/fa-star/#ffb020), +Lv10 Infinity Apex (300000/8.0x/fa-infinity/#34d399). PERK_CATALOG +9/+10. maxXp capped 9999999.
- FRAMES +9 Nova Vortex(frame-ascendant ☄), +10 Infinity Apex(frame-infinity ♾); CSS: frame-ascendant (amber conic/dotted halo), frame-infinity (emerald conic), static only.
- NEW EXTRA_FRAMES (selection-able): neon 'Neon Circuit' (5 spins, cyan/violet), galaxy 'Galaxy Rider' (20 spins, deep blue cosmos) + CSS + FRAME_DECOR_EXTRA {neon:2-style, galaxy:4-style}. getActiveFrame/buildAvatar/buildProfileAvatar +forceKey param; previewExtraFrame/selectExtraFrame (spin-guard, 0/5, 0/20 shown); showcase renders rank cards + extra cards + reset row.
- SPIN v5: SFX WebAudio engine 'sfx' (oscillator ticks rising during spin 44x120ms, win arpeggio, jackpot arp) localStorage 'sfx21' mute toggle btn in modal (sfxToggle). Wheel: 5s cubic-bezier(.08,.82,.22,1), new .wheel::before conic shine sweep (wheelShine 2.4s), center .whub 🎰 hub, deeper shadow. Ticks+win/jp chime cued.
- FEATURES: 2 spin achievements (id7 'চাকা পাগল' 3 spins+30XP, id8 'জ্যাকপট চেজার' 10 spins+60XP) w/ base 'spins' mapping in achCur/achTarget; LIVE level-progress bar chip on Home bonus area ('xpLevelChip', updates in renderBonusArea w/ next-rank hint + gradient bar).
- node --check SYNTAX OK; style braces 281/281; local+LAN+pinggy HTTP 200.
