# Session — 2026-08-15

## Device
- **Phone:** OPPO CPH2817 (MAIN DEVICE) — Android 16, aarch64
- (Second phone: Samsung A05m)

---

## 22:3X — .so File Reverse Engineering (MT2/apks)

### User Request
PATH: `/storage/emulated/0/MT2/apks/` — duita .so decompile kore dekhte bola holo ki ki ache.
(MT2 folder = MT Manager app er recycle bin; .recycle folder e porche.)

### Files Analyzed
1. `libandroidx.graphics.path.so` — 10,096 bytes — ELF64 ARM aarch64, stripped, Android 21, NDK r25c (clang 14.0.7)
2. `liblinphone.so` — 28.5 MB — ELF64 ARM aarch64, stripped, Android 28, NDK r27

### Analysis Tools (Termux)
- `file`, `readelf`, `llvm-nm -D`, `llvm-objdump -d/-s`, `strings -a`, `llvm-readelf -r`

---

### libandroidx.graphics.path.so — COMPLETE DECOMPILE (فرانسیس not needed, full logic decode)

**What it is:** Official **AndroidX Graphics Path** native library (`androidx.graphics.path` package) — open-source Android library. NOT malicious, standard Google lib.

**Exported:** only `JNI_OnLoad` (+ version node LIBANDROIDX.GRAPHICS.PATH)

**JNI_OnLoad (0x16c8) flow (decoded):**
1. `vm->GetEnv(env, JNI_VERSION_1_6)` at 0x16f4
2. `FindClass("android/graphics/Path")` → gPath (store @0xfd0 GOT)
3. `GetStaticFieldID(gPath, "mNativePath", "J")` → gNativePathField (0xfd8)
4. `FindClass("androidx/graphics/path/PathIteratorPreApi34Impl")` → 0xb43
5. Reads system property `ro.build.version.sdk` (0xb2e) + `atoi`
   - sdk >= 26 → RegisterNatives(7 natives) table @0x5c48/.data.rel.ro (via rodata @0xcf0 addressing)
   - sdk < 26 → different table @0x5cf0 (legacy layout)
6. `FindClass("androidx/graphics/path/ConicConverter")` (0xab1), RegisterNatives(1): internalConicToQuadratics

**Native method table (JNINativeMethod = name/sig/fnptr, resolved via .rela.dyn R_AARCH64_RELATIVE):**

| Java method | Sig | Native fn @ |
|---|---|---|
| createInternalPathIterator | (Landroid/graphics/Path;IF)J | 0x1888 |
| destroyInternalPathIterator | (J)V | 0x19d4 (→free) |
| internalPathIteratorHasNext | (J)Z | 0x19dc |
| internalPathIteratorNext | (J[FI)I | 0x19ec |
| internalPathIteratorPeek | (J)I | 0x1a80 |
| internalPathIteratorRawSize | (J)I | 0x1ab0 |
| internalPathIteratorSize | (J)I | 0x1ab8 (→0x13c8 big switch) |
| internalConicToQuadratics (ConicConverter) | ... "J" | 0x1ac0 |

**Key implementation details:**
- PathIterator struct (0x238 bytes, malloc @0x192c): offsets 0x0/0x8/0x10 = native handles (x20/x23/x24 copied from mNativePath, GetDirectBufferAddress), 0x18 & 0x1c = sizes, 0x20 = close flag, 0x21 = mode, 0x24 = tolerance (float), 0x28 = segment counter, 0x234 = queue counter
- SDK-version-dependent struct layout (three layouts: sdk>=26, 26..30, <24 switch at 0x1990/0x19b0)
- 0x13c8 = path segment type switch (6 types, jump table @0xaa0): handles SEGMENT types, computes sizes for floats
- 0x1234/0xec0 = recursive conic → quadratic subdivision (level selection based on tolerance; pow/log math at 0xd7c). Approximation level: fits tolerance via 4-sqrt comparisons
- 0x1ac0 internalConicToQuadratics = converts conic to quadratics using NewByteArray/SetByteArrayRegion/GetByteArrayRegion float arrays → outputs float[] of quadratics
- Float math NEON SIMD (ldp/stp paired floats), __stack_chk_fail everywhere (protector enabled)

**Verdict:** 100% stock AndroidX graphics-path. No custom code. (Matches upstream androidx.graphics.path JNI)

---

### liblinphone.so — OVERVIEW (60,148 exported dynsym, 346 imports)

**What it is:** Official **Linphone Android SDK** (`org.linphone.core` JNI bindings) — open-source SIP/VoIP library by Belledonne Communications. Statically bundled with many libs.

**Bundled libraries (categorized from dynsym):**
- linphone core: 4193 `linphone*` symbols
- **belle-sip** (SIP stack): 1455 `belle_*`
- **mbedtls** (TLS/crypto, e.g. MBEDTLS_*): 1039
- **mediastream2 (ms2)**: 802 `ms` + 77 `ortp_*` (RTP/rtcp)
- **libxml2**: 1020+ (`xmlXPath*`, `xml*`)
- **SQLite3**: 272 `sqlite*`
- **srtp/libSRTP**: 148 (SRTP media encryption)
- **speex**: 87, **opus**: 47, **g722**: 6 (audio codecs)
- bctoolbox utils: 344 `bctbx*`

**JNI:** `JNI_OnLoad` @0xe51a24 + `Java_org_linphone_core_*` full bindings back.

**Version hints:** belle-sip lib ~5.4.30, opus 1.3.0-motley, flexi-api-client + account_creator present (Linphone 5.x era SDK).

**Custom checks:** searched for app-specific domains/URLs/paths — only default linphone.org XSDs; NO custom endpoints. Stock SDK, not tampered.

**Verdict:** Official unmodified Linphone SDK .so extracted from an app that embeds Linphone (e.g., a VoIP/SIP calling app), sitting in MT Manager recycle bin.

---

### Tools recap for .so analysis (remember)
- `llvm-nm -D --defined-only lib.so` → exported symbols
- `llvm-objdump -d --no-show-raw-insn` → disasm
- `llvm-readelf -r` → relocations (needed to resolve .data.rel.ro pointer tables!)
- `strings -a` → strings with offsets
- Binary plist: `llvm-objdump -s -j <section>`

### Result to user
- Duita'i official open-source stock library. Choto ta complete reverse engineer hoye gese (native tables + struct layout + conic→quadratics algorithm). Boro ta Linphone SDK — feature list dekhanu hoyechilo.
- Kono custom/malicious code nai.
## 23:30-23:45 — Project 607: Telegram Remote Control System (Sketchware Pro)

**User request:** 2 ta phone (basay + haat e). Telegram bot diye bashar phone control korte chaile (emergency). Platform: Sketchware Pro (daydream v7).

**Context:** Project 607 (`mata.pro`) already ekta spy-style app (SMS + call + notifications → Telegram bot, server.json e token). Package: mata.pro.

**Ekhon build kora holo (files at `.sketchware/data/607/files/java/`):**
- `TelegramRemoteService.java` — foreground service + long-polling `getUpdates` (offset tracking, timeout=25, readTimeout 70s). Commands: /loc (sendLocation), /pic (Camera deprecated API photo→sendPhoto), /video N (MediaRecorder camera→sendVideo), /audio N (mic→sendVoice), /batt (battery+network+IP), /ring N & /ring stop (alarm max vol), /flash on|off (torch), /sms (content://sms inbox last 10), /ip, /help.
- `AutoStartReceiver.java` — BOOT_COMPLETED / MY_PACKAGE_REPLACED / USER_PRESENT e service + permission ask.
- `PermissionHelperActivity.java` — transparent activity, runtime perms (CAMERA, RECORD_AUDIO, FINE/COARSE LOCATION, POST_NOTIFICATIONS, SMS, phone state) request.
- `MyNotificationListenerService.java` — `onListenerConnected()` e jurechi service start + perms ask.
- `app_components.txt` — service (foregroundServiceType camera|microphone|location, exported=false), AutoStartReceiver, PermissionHelperActivity added.
- `permission` file — CAMERA, RECORD_AUDIO, FINE/COARSE LOCATION, FLASHLIGHT, VIBRATE, WAKE_LOCK, POST_NOTIFICATIONS, FOREGROUND_SERVICE_CAMERA/MICROPHONE/LOCATION, WIFI state er permission add.

**Ke jane:**
- FGS while-in-use camera/mic/location background e kaj kore (Android 14 e type-declared FGS dorkar — kora ache).
- Screen capture latest Android e no-go without MediaProjection consent (skip kora).
- Live streaming Telegram Bot API e nei — so "live" ≈ on-demand photo/video clip.
- Android 14 weapon battery killer — battery optimization off recommended setup step.
- view/logic binary files e dhora jay ni (main UI logic decode kori ni).

**Build pending:** user ke Sketchware Pro e project 607 open → run build → APK install home phone e.
**Setup steps recorded:** notification access + battery optimization off + perms grant.

## 23:45-23:55 — Install fail debug (Project 607, OPPO CPH2817 / Android 16)

**Error:** "App not installed as package appears to be invalid" (INSTALL_FAILED_INVALID_APK)

**Root causes (APK check diye dhora):**
1. `SBD_release.apk` signer cert DN = `CN=Android` → **AOSP test key** → OPPO/ColorOS (Android 16) AIOCC test-key apk block kore.
2. `attributes.json` e 2 ta bogus uses-permission: `android.provider.Telephony.SMS_RECEIVED` (action name! permission na) + `NOTIFICATION_LISTENER_SERVICE` (nai).
3. `targetSdk=34` (aapt badging confirm) but TelegramRemoteService selected `foregroundServiceType` nei → Android 14+ e MissForegroundServiceTypeException runtime crash. Ekhon `camera|microphone|location` re-add.
4. Android 12+ requirement — GetSmsAndCalls + MyNotificationListenerService e `android:exported` nei → added.

**Tools used:** aapt dump badging, apksigner verify, unzip + strings class check, keytool. Apk at `/storage/emulated/0/sketchware/signed_apk/SBD_release.apk`.

**Fix done:**
- custom keystore: `/storage/emulated/0/sketchware/keystore/devzeron_2026.jks` (CN=DevZeron, pass `devzeron123`, alias devzeron). Re-sign verified (apksigner output CN=DevZeron).
- attributes.json cleaned, app_components fixed (exported + FGS).
- Pipeline: after rebuild → `apksigner sign --ks devzeron_2026.jks --ks-key-alias devzeron --ks-pass pass:devzeron123 --key-pass pass:devzeron123 --out SBD_signed.apk SBD_release.apk`
- User rebuild → re-sign → INSTALL.

**Key learning:** OPPO/realme (ColorOS) build-er default test key (CN=Android) sign-apk install korte dey na. Custom keystore dorkar. Termux e keytool available.

## 23:57 — Re-sign done (user rebuild pore)
- `SBD_release.unsigned.apk` (new build, fixed manifest) → `apksigner sign` custom devzeron_2026.jks → `SBD_devzeron.apk` (8.34MB)
- Verify: v1+v2+v3 true, cert DN CN=DevZeron. Bogus SMS_RECEIVED permission count 0 (attributes.json cleanup successful).
- Path: /storage/emulated/0/sketchware/signed_apk/SBD_devzeron.apk → user install korben.
- NOTE: Android 16 te FGS camera/mic/location + runtime perms dorkar; PermissionHelperActivity handle korbe. Boot e FGS start Android 15+ restricted — user open app / notification access restart korle service uthbe.

## 00:00-00:25 — V2: crash fix + feature batch + dead token discovery

**Research discovery (CURL):** bot token 7684995533:AAEg... in server.json e **401 Unauthorized** → DEAD token. Eitai "kono command kaj kore na" er root cause (reply/action kothao jacchilo na). User ke @BotFather theke new bot token dite bolbo.

**Crash fix:** MyNotificationListenerService.onNotificationPosted line 48 `extras.getString` → NPE (extras bundle null). Full rewrite: extras null-guard, whole method try/catch, self-package skip, dedupe (key+postTime set 300), owner-aware routing.

**Feature batch (TelegramRemoteService V2):**
- Owner auto-claim: 1st message claim → reply target dynamic (ownerId>0?owner:chatId). server.json chat_id fallback.
- `/status` — service, owner, lost mode, admin active, notif listener, battery opt, overlay perm, battery %.
- `/lock` (DevicePolicyManager.lockNow, needs admin), `/admin` (ACTION_ADD_DEVICE_ADMIN), DeviceAdminReceiver + @xml/device_admin (force-lock policy) + BIND_DEVICE_ADMIN.
- `/lockmsg <text>` fullscreen overlay alert (SYSTEM_ALERT_WINDOW TYPE_APPLICATION_OVERLAY), `/closemsg`.
- `/lost [min]` lost mode: periodic location + on SCREEN_ON photo+loc auto send; `/found` off.
- `/pic`, `/pic front`, `/selfie`, `/video N`, `/audio N`, `/batt`, `/ring`, `/flash`, `/sms`, `/ip`.
- `/optsetup` battery-opt exemption, `/overlay` overlay permission, `/claim` re-owner.
- Persistence: KeepAliveReceiver (AlarmManager inexactRepeating 5min → restart service), onTaskRemoved restart, GetSmsAndCalls watchdog on sms/call/boot/screen, SCREEN_ON punch hook.
- New files: DeviceAdminReceiver.java, KeepAliveReceiver.java, files/resource/xml/device_admin.xml.
- permissions += BIND_DEVICE_ADMIN, REQUEST_IGNORE_BATTERY_OPTIMIZATIONS, SYSTEM_ALERT_WINDOW, CHANGE_NETWORK_STATE.
- manifest: SCREEN_ON/USER_PRESENT/BOOT on GetSmsAndCalls, DeviceAdminReceiver (meta device_admin), KeepAliveReceiver.

**Known limit (Android 15/16):** boot-er somoy camera/mic FGS background-start nishedh → reboot-er por ekbar app open dorkar; nahole SMS/screen-on/notification connect revive. KeepAlive alarm best-effort.

**Next user action:** @BotFather new bot token → server.json e bot_token replace → Sketchware rebuild → apksigner custom sign → install → setup (perms/admin/battery opt/overlay) → /start link.

## 00:20-00:40 — V3 fixes + findings
- **server.json updated by user:** token now `8125197063:AAHEcRBr-...` = @ZeronLogBot VALID (curl getMe ok), chat_id 5271912123. OLD apk e still dead token baked → rebuild jarur.
- **release_key.jks = DevZeron key now!** (keytool: alias devzeron, CN=DevZeron, pass devzeron123, PKCS12). Means Sketchware keybox replaced → builds auto DevZeron signed. devzeron_2026.jks file gone (moved to release_key.jks). Script updated key-aware.
- **Device admin myth:** phone theke challenges Settings→Security→Device admin / /admin command. No computer needed.
- **V3 code adds:** permission self-check per command (requirePerm), /diag (perms+flags report), /burst N, /apps, /wifi, /maxvol, /sim; SIM change alert (sharedprefs KEY_SIM), boot notify, power connect/disconnect alerts (manifest POWER_CONNECTED/DISCONNECTED), lost mode extra photo on power.
- **compile-check passed** (android-34.jar + okhttp/okio + javac) 28 classes clean.
- User still confusing: "theke command dewa lagbe PC te" — clarified + /admin.

## 01:00-01:25 — V4 fixes: camera/flash/video/persist + overlay UX (Project 607)
- **Compile check:** android-34.jar + okhttp/okio javac → 29 classes clean.
- **Flash rewrite (`handleFlash`)**: CameraManager.setTorchMode (SDK≥23) e switch kora holo — Camera.open() chhara torch chalano jay, no preview needed, OPPO te reliable. Fallback `legacyTorch()` (Camera API, FLASH_MODE_TORCH + startPreview).
- **Photo fix (`takePhotoAndSend`)**: `setPreviewTexture(SurfaceTexture)` add (Camera.open kono surface chara preview fail kore OPPO te). 6s sleep block remove. Retry loop (attempt 0 → busy → sleep 1.2s → retry, 2nd fail → error msg).
- **Video fix (`recordVideo`)**: `rec.setPreviewDisplay(new Surface(SurfaceTexture(12)))` add — camera → MediaRecorder chhara alada setup hole e preview display dorkar. cam.unlock() already thakche.
- **/lockmsg UX:** permission nai → auto `openOverlaySettings(chat)` (popup e allow) + reply guide. Fullscreen upgraded: 🚨 DEVICE ALERT 🚨 title + msg, `startBlink()` alpha blink 600ms + FLAG_KEEP_SCREEN_ON.
- **/maxvol:** + `setInterruptionFilter(INTERRUPTION_FILTER_ALL)` → DND off, muted hole o full ring (Android 16).
- **Persistence:** KeepAliveReceiver interval 5min → **3min**; AutoStartReceiver boot/replace/user_present e now `KeepAliveReceiver.schedule()` too — alarm sehaab ashbe, FGS start fail holeo.
- **Next user action:** Sketchware rebuild → apksigner sign (release_key.jks) → install → `/diag` diye verify + camera/flash/lockmsg test.

## 02:00-02:40 — V5: notif spam fix + button system + /apps menu + video/lockmsg reliability (Project 607)

**User complaints:** (1) notification repeat/spam (`lop`) bot e. (2) `/video 10`, `/lockmsg`, `/closemsg`, `/maxvol` kaj kore na. (3) overlay permission popup ase na (manually dilyeo kaj kore na). (4) `/apps` boring plain list → dynamic button menu chai. (5) Sorbomot buttons diye control chai.

### Notification dedupe rewrite (`MyNotificationListenerService.java`)
- Consistent looping logic remove kora holo: persistent SharedPreferences (`notif_prefs`) content-hash dedupe.
- Same notification key + same title/text hash → **10 min** window e repeat skip.
- Per-package cooldown (default 4s, pref `notif_cooldown_ms`) — rapid re-post block.
- `notif_enabled` pref toggle → service theke `/notif on|off` control.
- Icon dump (giant `largeIcon.toString()`) bade. Title+text khali thakle skip.

### LockAlertActivity (NEW) — overlay chhara fullscreen alert
- Overlay permission nai thakleo `/lockmsg` kaj kore: FGS theke fullscreen activity launch (Activity start FGS exemption).
- SHOW_WHEN_LOCKED + TURN_SCREEN_ON + KEEP_SCREEN_ON + bright, red alert blink.
- static `show()`/`finishAlert()`; `/closemsg` + `removeOverlay()` dui jaygay call hocche.
- app_components.txt e activity register kora.

### Button system (Telegram inline keyboard)
- `/menu` → Control Panel keyboard: loc, pic, front, flash on/off, video 5/15/30s, audio 15s, ring 20/stop, batt, wifi, status, lock, lost, found, apps, notif on/off, close.
- Poll loop e `callback_query` parser add → `answerCallbackQuery` + dispatch.
- `handleCallback` — data codes `do:*`, `vid:N`, `aud:N`, `apps:page`, `app:idx`, `app:act:idx:launch|info`.
- helpers: `sendWithButtons`, `buildKeyboard` (JSON inline_keyboard).

### /apps dynamic menu
- Non-system apps sorted by label → paginated inline keyboard (8/page, Prev/Next/Close).
- Click → app details message: label, package, version, **granted permissions (✅/➖, 25 show)**, first install date.
- Buttons: Launch / App Info (Settings detail page) / back to list.

### /video reliability
- 4 fallback encoder combos (H264+AAC → H264 no-audio → MPEG_4_SP+AMR → H263 no-audio).
- frame rate fixed 20 remove (unsupported thaile prepare fail hote).
- "/video N" sec limit 60; progress message before; real error msg pore.

### Status
- javac 33 classes clean. Rebuild → sign (release_key.jks) → install dorkar.
- Commands changed: `/help` e /menu + /notif mention. Old `/apps` plain list gone.
- NOTE: `/maxvol` logic thik ache — test pore apar result dekhte hobe (user rebuild korini mone hoy).

## 03:00-03:30 — V6: lockmsg permission-free + keyboard diagnostics (user live-test findings)

**User live test (V5 build) feedback:**
1. `/lockmsg vgh` → reply "show hoise" **but phone e kono alert na** — overlay dejho thik kore na (OPPO te addView silent fail ba same-permission issue). User chay **permission nai nileo** kaj korbe.
2. Control Panel message ase **kintu kono button na** ("inline_keyboard research koro").
3. `/apps` o kaj kore na (no buttons).

**Research/Test (curl + live API):**
- Exact markup format app theke bani — `reply_markup={"inline_keyboard":[[{"text":"📍 Location","callback_data":"do:loc"},...]]}` → **Telegram API 200 OK, buttons server-side attached** (test message 4860). Format SOB-THIKE thik. Mane APK e jodi V5 thake buttons ashbane — kintu user sebab NA. Suspect: (a) build holo kintu purono? (b) in-app j vol error. => Diagnostics add.

**V6 changes (`TelegramRemoteService.java`, `LockAlertActivity.java`):**
- `/lockmsg` now **always activity-first** (permission chara fullscreen real Activity — FGS theke allowed; works screen-locked: SHOW_WHEN_LOCKED + TURN_SCREEN_ON). Overlay = backup jodi activity start fail. `LockAlertActivity.showSafe()` boolean return.
- `sendWithButtons` + **error surfacing**: Telegram 400/network fail hole oi error text-ew owner chat e reply (in-app k haticholo dhora jabe) + `buildKeyboard` thik na hole plain text fallback.
- `answerCallback` optional feedback text; /pic button click e "📷 Photo nicchi…" reply.
- compile clean 34 classes.

**Remaining mystery:** buttons na asha — diag message na ashle user keep Telegram update / fresh rebuild required. Next: user rebuild V6 → install → /menu test → any "⚠️ Button send error" report kortei hobe.

## 04:00-04:45 — V7: ROOT CAUSE FOUND (buttons) + lockmsg = EMI-style banner (user final redefinition)

**User redefine /lockmsg:** Fullscreen block NAI chai. Chai jonno kistir phone er moto — **phone chalano jabe, but screen e constant message thakbe** (ja user likhe dibe). And SELLINGS never permission.

**ROOT CAUSE — but app buttons na ashche (V5/V6 body):**
- Java `Object[][] rows = { {"📍 Location","do:loc"}, ... }` → inner arrays **Object[]**, SOB NEVER `String[]`.
- buildKeyboard used `o instanceof String[]` → **always false** → keyboard skeleton `{"inline_keyboard":[]}` → **Telegram text pathay but buttons na**!
- Verified live: host pure-Java test (2 tools), exact JSON curl → API 200 ₸ buttons. Root cause definitively Java literal votrench.
- **FIX:** rows flat-pair interpretation `[text,data,text,data,...]`, JSON manually build (no org.json), `jesc()` escape helper. Live te "✅ Buttons fix confirmed" (msg 4901) test pass.
- Learned file saved: `learned/2026-08-15-object-array-literal-inline-keyboard.md`.

**lockmsg redesign (EMI banner):**
- FGS notification text → lock message (permissionless, status bar + shade).
- Overlay thakle **top banner stripe** (WRAP_CONTENT, FLAG_NOT_TOUCHABLE → phone usable, blink).
- /closemsg → banner + notif restore. LockAlertActivity chuara (unused).
- `startForegroundCompat` refactor → `buildNotif(title,content)` + `updateOngoingNotif()` reusable.
- compile clean 34 classes.

**Next:** user rebuild V7 → install → /menu (buttons ashbe!), /lockmsg msg (banner+notif, phone usable).

## 05:00-05:50 — V8: screen share + screenshot + features batch (MediaProjection) + lockmsg heads-up

**User demands:** lockmsg "reply ashe but kaj kore na" (screen e visible chai) + /apps totale kaj kore na + **SCREEN SHARE + SCREENSHOT** + "aaro ja ja kora jabe koro".

**V8 changes:**
1. **lockmsg visibility (3 layers):**
   - (A) HIGH heads-up alert notification (channel `remote_alert`, IMPORTANCE_HIGH, BigTextStyle, CATEGORY_ALARM, PUBLIC) — screen e popup, **permissionless**, lock screen eo ashe.
   - (B) FGS notification text e message.
   - (C) Overlay thakle top red banner (return boolean; reply dekha jay banner on/off).
   - /closemsg → banner + alert notif cancel + FGS notif restore.
2. **Screenshot `/shot` + Screen record `/screenrec N`** (MediaProjection):
   - `ScreenCaptureActivity` (transparent) → createScreenCaptureIntent consent → `onProjectionResult()` static → thread.
   - Screenshot: ImageReader RGBA_8888 → bitmap → jpeg → sendPhoto.
   - ScreenRec: MediaRecorder SURFACE source + VirtualDisplay → mp4 → sendVideo.
   - FGS type `mediaProjection` add (components) + FOREGROUND_SERVICE_MEDIA_PROJECTION, USE_FULL_SCREEN_INTENT permissions.
   - VIRTUAL_DISPLAY_FLAG_AUTO_MIRROR — manual jaei VirtualDisplay.FLAG nai (compile hit lage — DisplayManager constant diary).
3. **New commands:** /contacts (READ_CONTACTS), /call (CALL_PHONE), /sendsms (SEND_SMS), /vibrate N, /screenon N (wakelock), /bright N (WRITE_SETTINGS flow), /apps list (plain fallback), /apps try/catch.
4. **Menu buttons:** 📸 Shot, 🖥 Rec 10s, 👥 Contacts, 📳 Vibrate, 💡 ScreenOn 30s.
5. PermissionHelperActivity CRITICAL += READ_CONTACTS/CALL_PHONE/SEND_SMS.
6. compile clean **37 classes**.

**Remaining notes:**
- /apps buttons fixed V7 (flat-pair) — user V7 test holo na, V8 rebuild e test hobe.
- MediaProjection consent — prottek /shot /screenrec e dialog ashe (Android rule), 'Start now' tap lagbe. First time e screen bhag shakor via /optsetup + notif access thakle auto.
- /sendsms modern Android e default-SMS app na hole fail hote pare — expected.

## 06:00-07:20 — V9: website + Railway live dashboard + file manager (ZeronRemote)

**User demand:** live camera both cams + live screen website e dekhte chai, website theke command, file manager, incoming data site e. Railway e host korte chai.

**Work done:**
1. App (Telegram) e file manager: /files (page buttons, flat cells — ArrayStoreException fixed), /zip <path>, /allfiles (MANAGE_EXTERNAL_STORAGE), sendDocument, menu 📁 Files button, help allfiles/zip lines. Permission file += MANAGE_EXTERNAL_STORAGE. Compile 41→53 classes clean.
2. `~/zeronremote/` server (zero-dep, Railway). All endpoints curl-tested local ✓.
3. `RemoteServerClient.java` (mata.pro) — poll 4s, full command dispatch, live cam/screen streaming, file list/download/zip, data snapshots. /server set <url> <key> runtime config (rc_prefs) — no rebuild.
4. Service hooks: rsc init/stop + onProjectionResult stream/shotweb branches (chat=-1 web mode).
5. assets server.json += server, device_key (empty default = disabled, safe).

**Compile:** clean 53 classes.
**Server tests passed:** dash 200, cmd→poll, data r/w, frame→live jpg 26B, fget once-serve 200/404.
**Remaining:** user deploy → Railway + DEVICE_KEYS env → /server set → test live.

## 07:25-07:45 — DEPLOYED: ZeronRemote LIVE on Railway

- User: "site e kichu nai, file upload koroni". Placeholder URL diyechilam (apnar-secret-key) — ekhon real deploy.
- **Done:** `~/zeronremote` → git repo → GitHub `ZeronModz/zeronremote` (private, master). Railway project `zeronremote` (DevZeronXs) + railway up -y deploys. Env `DEVICE_KEYS=zrn-3409b9fac69f` set (abar service thakbe na bole 1st set ray hoye nai — redeploy -y kore auth verify).

**LIVE:** https://zeronremote-production.up.railway.app/?k=zrn-3409b9fac69f
- no-key→401 ✓, good-key cmd/poll round-trip ✓ (VPN/bg keepalive off thakleo ok, poll 4s).
- Phone: `/server set https://zeronremote-production.up.railway.app zrn-3409b9fac69f`
- User kaaj: rebuild/install app → kam set → site chalu.

## 07:50 — assets/server.json filled (auto-connect, no /server set needed)
- device_key user nije dice (already set); server "" → real URL set: https://zeronremote-production.up.railway.app
- JSON valid ✓. Rebuild korlei RemoteServerClient auto start (loadConfig reads assets fallback).

## 08:00-08:30 — round 2: phone CONNECTED + /sharescreen web_app + camera frame fix
- **Diagnosis:** phone connect hoise ekhon! /api/data batt → "4% 34°C unplugged" (round-trip OK). Site status=null age (redeploy memo wipe). Rail: server data memory, redeploy e sab dhoye.
- **Bug found:** live camera camstate="back live" kintu frames 404 → onPreviewFrame fire korto na (SurfaceTexture(0) consume na). Fix: SurfaceTexture(10) + consumer loop updateTexImage (30ms) + setPreviewCallbackWithBuffer (2 buffers) + framesSent counter + cameraError debug post. 54 classes clean.
- **New feature /sharescreen:** Telegram bot e (a) /sharescreen command, (b) menu "🖥 Share Screen" do:share → inline_keyboard web_app button → opens `/?view=screen`. sendWebAppButton() JSON reply_markup. RemoteServerClient.webAppUrl() getter.
- **Server:** `/?view=screen` single-screen live page: auto `cmd("live screen on")` ta 0.6s e, MJPEG img + reconnect fallback (onload lastImg, 3s stall → re-image stream), dev-status banner (connected/disconnected), /api/sys heartbeat (lastSeen updated on poll/frame/dataPOST). Deployed.
- **User need:** REBUILD app (Sketchware) → install → /menu → 🖥 Share Screen → web-app button → phone e "Start now" → live screen.
- cam test: push "live camera on" then /api/live/cam.jpg.

## 08:45-09:10 — social-media-style live: /shot site-push + smooth stream
- **User:** "/shot dile screen share hoy kintu website e nei" + MJPEG niye doubt + chai social-media style smooth live via web_app.
- **Fixes:** (1) doScreenshot → rsc.publishScreenFile(f) → /api/frame/screen → /shot result site eo furnace dekha jay. (2) screen stream: throttle 350→100ms (~10fps), sleep 20, imageToJpegScaled 540p q56 (big frame pathaye smooth). camera live: 300→130ms q45. screendbg frames debug o.
- compile 54 classes clean. **User REBUILD dorkar** — installed build e ei fixes nei.
- MJPEG = flipbook (img). Sosial-style smooth = 10fps 540p. WebRTC (meet-like HD) — future.

## 09:30-10:15 — FINAL: zeronremote v2 full redesign + heartbeat + all verified
- **Server v2** (`server.js` rewrite): glassmorphism dark UI — header pill, live stats strip (battery/model/android/ram/storage/ip from /status auto-parse), Camera + Screen live cards with ●LIVE badges + auto-reconnect (3.5s), Control pad, tabbed Data center (status/contacts/sms/calls/notifs/batt, rendered tables), file manager w/ breadcrumb chips + open/download/zip (encodeURIComponent safe), command log + toasts, Setup card. New `/api/all` bundle. MJPEG stream push now also on setInterval keep-alive (1.2s repeat latest) — stream survives phone quiet. Blob TTL 10min. auth still 401 no key.
- **App:** auto-heartbeat thread (status+batt every 30s) so dashboard stats always fresh. 55 classes, 0 lambda.
- **Deployed+verified live:** dash 200, screenpage 200, /api/all ok, cmd→batt roundtrip (5% 36°C!), no-key 401.
- **User: REBUILD app still needed** for: camera frames fix, /sharescreen web_app button, /shot→site push, 10fps smooth, heartbeat.

## 10:30 — camera live → camera2 (backup: legacy Camera blocklist e atkaye)
- Bug: OPPO Android 16 te legacy Camera.setPreviewCallbackWithBuffer block/hang → 0 frames (cameraError alart ageo chilo).
- Fix: RemoteServerClient e startCamLive full camera2 rewrite + stopCam() → stopCamLive + new fields (camDev/camSession/camReader ImageReader YUV_420_888 max 3/camHt HandlerThread).
- Py: pickCamId (LENS_FACING), pickCamSize (SCALER_STREAM_CONFIGURATION_MAP min res ≥320), yuv420toNV21 (uvPs 2 interleave + 1 planar duita case handle), fps 130ms q45 same.
- Legacy Camera import rekhechi (snapCamFrame/setFlash aro legacy use). SurfaceTexture import bad (again).
- Efaultke: camera2 API 21+, error messages cameraError post. Build: 290/290 braces OK. User REBUILD dorkar.

## 10:45 — camera2 compile fix (2 bugs)
- ImageFormat actually `android.graphics.ImageFormat` (NOT android.media.ImageFormat) → import fix.
- Handler er jaygay Looper pass hoyechilo (setOnImageAvailableListener/openCamera/createCaptureSession/setRepeatingRequest sob Handler chay) → `final Handler h = new Handler(ht.getLooper())` + rb final. Braces 294/294.
- Simpler rule: camera2 API sob Handler ney, Looper na.

## 11:30 — web_app full online recharge: cam front/back + online file manager
- **User:** website "kaam nei", sob bot e /sharescreen er moto web_app e hoy, /livecam f/b alada alada, file manager online chai.
- **Server server.js:** servePage view routing + `camPageHTML(face)` (front/back seg, auto `live front/back`, MJPEG cam stream, key-preserving hub links) + `filesPageHTML()` (online file manager: list/open/download/zip/crumb, reuse files data key). Hub e "📷 Cam ↗" "📂 Files ↗" links. node --check OK, local 200, Railway deploy OK (cam/files 200).
- **Bot TelegramRemoteService:** `/livecam [f|b]` → webAppButton(s) `?view=cam&face=front|back`; `/livecam` (without arg) → 2 web-app buttons front+back; `/filesweb` → `?view=files`. Menu + "📷 Live Cam" "📂 Files Web". Help text update.
- **RemoteServerClient:** webAppUrl() → overload webAppUrl(view, extra).
- Braces: RSC 295/295, TG 667/667. **User REBUILD app dorkar** (bot changes java-te).

## 12:30 — v3 M3 redesign + Material You green theme + research — DEPLOYED
- **Research (web):** MJPEG+conflated latest-frame = best remote monitoring; WebRTC needs TURN/SFU infra; HLS 2-40s latency. Architecture already right: server keeps latest frame per src + fan-out; only pump timer 1200→650ms lagged.
- **Full server.js rewrite → v3:** single M3 design system (CSS tokens + components: appbar, bnav bottom nav 4 views, seg control, cards/stats/btns FILLED/TONAL/OUTLINED/ERROR, chips, tabs, snackbar, stream health badge). Views: hub (auto camera start? no—manual, auto status/batt/stats, tabs for data), cam front/back seg + btn, screen, files (auto-list on load). MJPEG stream pump: conflated write-when-new 650ms.
- **Theme:** user gave /storage/emulated/0/Download/material-theme.zip → css/ dark.css Material You tokens (primary rgb(177 209 138) soft green) — applied to M3{}.
- node --check OK; local 200 all views; deployed Railway; hub/cam/screen/files all 200 with theme; batt roundtrip OK; phone online.
-  Note: header pill status + bottom nav = strong M3. Screenshot/f12 inspect recommended.

## 13:15 — v3.2 static pages + custom SVG icons + premium cam + files bug FIX
- **BUG crave:** file manager ar hub data tabs kaj korto na karon frontend `U("/api/data&key=files")` (wrong ampersand) — server pathname e `/api/data&key=files` hoy → 404. Fix: `?key=` (correct query). `grep -c` verify deploy e.
- **Split 4 static HTML files** in `public/`: hub.html, cam.html, screen.html, files.html + shared app.css (M3 tokens) + common.js + icons.svg (custom stroke icon sprite ~24 icons, ZERO emoji).
- **Premium cam page:** corner focus brackets, live blink badge pill-top, fade bottom, infobox tags (face/fps), angle cards front/back with selection checkmark, toggle Start→Stop (btn filled→error), stream health card, MJPEG auto-reconnect.
- Screen/files pages: same chrome + bottom nav (Hub/Camera/Screen/Files). Screen page note MediaProjection "Start now".
- server.js: clean rewrite — static serving (no key for css/svg/js), PAGE_CACHE, conflated MJPEG pump 650ms. node --check OK. Local all views 200. Deployed Railway: hub/cam/front/screen/files 200, css/icons 200, cam 14 icon refs, files ?key fix present, 0 emoji.
- **Bot note:** /livecam & /filesweb & /sharescreen web-app buttons point `?view=cam&face=...`/`?view=files` → new pages auto. szobaicht: app REBUILD laagbe NA for site change (server-only). Camera still needs rebuild for camera2 (done earlier).

## 13:50 — cam.html isolation + Telegram fullscreen + toast fix
- cam.html e appbar + statusbar (#st) + bottom nav (bnav) — SOB remove. Shudhu camera stage + angle cards + stream health + control buttons.
- #pill hidden rakha ache (conn tracking): `display:none`, cue-pill `pill()` function e depend kore.
- Toast: common.js snack() timeout 3200→1600ms (2s er bhitorei chole jay).
- **Telegram requestFullScreen** (video: Siam Shekh requestFullScreen tutorial theke): load e `Tg.requestFullScreen()` + `Tg.expand()`, body class `fulltop`, `.wrap{margin-top:56px}` (fullscreen e content upore uthle niche anar jonno 50-60px offset).
- Verify local (PORT 7909): all views 200, banned elems 0, fulltop+requestFullScreen present. Deploy Railway: all 200, cam page fulltop+requestFullScreen live, toast 1600 live.

## 14:20 — fullscreen + toast FIX round 2 (root causes)
- **Fullscreen na horar karon:** Telegram er official JS method `requestFullscreen()` (lowercase s), code e silo `requestFullScreen()` (capital S) → undefined → silently fail (try/catch swallow). Fix: lowercase, plus `setHeaderColor("#12140E")`, `ready()`, `expand()`, `fullscreenFailed` fallback rene 600ms `fs()` retry, `viewportChanged` → `.fulltop` class.
- **Toast dur korar jonno:** Telegram webview aggresively cache kore (3200ms purano common.js). `Cache-Control:no-store` thakleo user ekhono purano toast dekhchilo. Fix: sob asset e cache-bust version query — `app.css?v=4`, `icons.svg?v=4#...`, `common.js?v=4` (sob 4 page e sed bulk).
- First deploy e old cam.html reh gechilo (capital S) — `railway up` er por 25s wait kore `grep` verify: 2x `requestFullscreen` ok. First `.to` wait shorter chilo.

## 14:45 — toast/log TIROTH fix: asol culprit #log!
- "live front" toast hi jai na — karon `logMsg()` diye #log element e "→ live front" append hoy, kintu #log items auto-hide korte na, bottom e boshe thakto.
- Fix: common.js `logMsg()` → 1700ms logic e `.ln.out` class (opacity 0 transition .35s) tarpor DOM remove. app.css e `.ln.out{opacity:0}` + transition add.
- Fullscreen round-3: video er moto fullscreen na horar karon — official doc. `<script src="https://telegram.org/js/telegram-web-app.js">` add kora holo head e (doc: script tag head e dewa dorkar). `requestFullscreen()` lowercase s confirmed live 2x.
- Deploy verified: telegram-web-app.js 1x, requestFullscreen 2x, log autohide 1700ms + .ln.out present, all 4 pages 200.

## 15:10 — fullscreen offset 20px + files/screen page cleaning + robustness
- Fullscreen offset: `.fulltop .wrap{margin-top:56px}` → `calc(env(safe-area-inset-top,0px) + 20px)` — cam/files/screen 3 page e.
- files.html: appbar + statusbar + bnav remove, #pill hidden, telegram-web-app.js script, requestFullscreen, same .fulltop — local/test verified appbar=0 bnav=0.
- screen.html: same treatment.
- **File manager fix (round 2):** front-end e list reload robustness — `fillWithRetry()` 6 tries 2s interval (device slow postData / poll delay er jonno onek bar empty ashchil). `ch/ls/lsRoot/fsUp` sob fillWithRetry e.
- Screen share note: server stream endpoint already exists; device side postFrame screen. N.B.: app REBUILD dorkar screen-share + files for device-side (user side).
- Buffer complaint: deploy er sathe sathe first verify e stale pages dekhachilo (Railway warm-up) — eta ekarone `?t=<timestamp>` cache-bust query diye re-verify korte holei sob 200 pata.

## 15:40 — SCREEN SHARE FIX: MediaProjection foreground service error (Android bug!)
- **Error:** `Screen cap fail: Media projections require a foreground service of type ServiceInfo.FOREGROUND_SERVICE_TYPE_MEDIA_PROJECTION`
- **Root cause 1:** Manifest e `FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_MEDIA_PROJECTION` permission declared na chilo → attributes.json e add korlam (9th/10th).
- **Root cause 2:** `startForegroundCompat()` main branch 2-arg startForeground() (type chara) → Android 14+ e MediaProjection type saho start kora required. Fix: API 29+ e type combine (camera|mic|location|mediaProjection), API 34+ e 3-arg.
- **Root cause 3:** projection create korar age service foreground confirm hote hobe → `onProjectionResult` dui file-e e `startForegroundCompat()` call add.
- **/sharescreen flow change:** `shareScreenNow(chat)` — command dilei projection dialog, allow korlei rsc stream suru, 6s por frame na ele re-try, stream live hole webAppButton "🖥 Live Screen Dekho" pathay. (age sudhu button pathay, kaj shuru kore na)
- RemoteServerClient.e `isScreenLive()` public helper add.
- Compile test (javac + android-34.jar): shudhu okhttp lib missing — Sketchware e exists, amar code-e 0 error.
- ⚠️ **User: Sketchware e app REBUILD + install dorkar** — Java + manifest change (forces: attribute, args patano hobe na porjonto).
