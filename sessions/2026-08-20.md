
## 06:45 — TeraBox VIP Player (y.html) bugfix round 2 🐛
**Context:** user bolechhe ager fix eo kichhu cholche na. y-old.html = working old version (CDN-based).

**Root cause (the REAL bug):**
- y.html er `<script type="module">` er moddhe `function getFrameForUser` **duplicate** chhilo (line 1446 ang 1466) → `SyntaxError: Identifier 'getFrameForUser' has already been declared` → **ENTIRE module parse fail → sob kichhu dead**.
- Amonki ager hosted copy (~/terabox-vip/y.html) eo ei dup chhilo → tai user bolche "এখনো ঠিক হয়নি" ✅ confirmed.
- Additional: `limit` import chhilo na (leaderboard crash), local `libs/` folder exist kore ni (CDN e phire gelam, old working version er moto).

**Fixes applied (original `Download/Telegram/y.html` + synced to `~/terabox-vip/y.html`):**
1. Removed duplicate `getFrameForUser` (1 ta rakhlam)
2. Added `limit` to firestore import
3. Firestore/firebase imports → CDN `gstatic.com/firebasejs/10.8.1/` (old khobor chhilo)
4. FontAwesome → cdnjs 6.5.1, SweetAlert2 → jsdelivr `sweetalert2.all.min.js`
5. Fixed latent `feedCache is not defined` bug (empty-feed path) → `window.__feedSigMap` guard

**Verification (full):**
- `node --check` on extracted module → SYNTAX OK ✅
- Boot-simulation harness in node (document/localStorage/Swal/TG stubbed, firebase 10.8.1 local) → module import OK, 28/28 handlers defined, onload ran, **boot flags {"fb":true,"fs":true,"ev":true}** ✅
- CDN endpoints 200 ✅ | html tag balance OK ✅
- Hosted at http://localhost:8080/y.html (folder ~/terabox-vip, server running)

**Lesson (learned):** `function` declaration duplicate in old-module e SyntaxError to ir module parse kokhono bujhi — always `node --check` korbo extracted module, static ID check e r ghabrabo na. `limitation` noy — eta primary check.
