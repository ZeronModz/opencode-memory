# Session 2026-08-06 (OPPO CPH2817 MAIN)

## Telegram brain v10 — TRUE FAST-FIRST RACE (replaces v9 two-stage pipeline)
- User complaint: replies slow (was taking too long) / sometimes no reply. Agreed with concept of "whichever model replies first, send that first; if one fails the rest cover".
- ROOT CAUSE of slowness: v9 ran ALL models concurrently but `as_completed` loop waited for EVERY future (slowest ~30s), THEN ran stage-2 synthesis via a fresh editor model (another ~30s) → up to 60s+ per message. Traced in core.py `_run_all_concurrent`.
- FIX: new `_race_first(models, prompt)` (core.py:94):
  - Runs ALL models in PARALLEL (max_workers=min(len,4)).
  - `wait(FIRST_COMPLETED)` → the FIRST valid reply (len>2) wins → returns immediately → remaining futures CANCELLED.
  - No waiting for slow models, no stage-2 synthesis. One fast reply.
  - If all empty/timeout → 9th-brain orchestrator retry via llm_chat once → warm fallback. Never silent.
- `_handle_inner` simplified (replaced stage1+stage2 block): race TEXT_MODELS (+VISION if image), send winner.
- Verify: `python -m py_compile core.py` OK. Restarted single process PID 16066 (ps confirms ONE core.py). Online log: `[zeron] online as XASANN @XASSANNZ`.
- LESSON (learned/): "race all models, first-valid wins, cancel rest" is FAR faster than collecting-then-synthesize when user prioritizes speed. Don't wait for slowest. FIRST_COMPLETED + cancel is the pattern.
## Telegram v11 — TWO-MODE: fast chat race + full TEAM task mode
- User spec: normal chat = who replies first replies (fast). Big tasks (build website/app/code/design) = use ALL models together, each handles one part (design/logic/QA/content), then merge best into one reply. Plus animated "working" status feel (like terminal).
- Added (core.py):
  - `detect_task_type` — classifies message: needs both ACTION word (build/create/banaw/likh/তৈরি...) + TARGET word (website/app/code/bot/game/tool...) → returns web/app/code/general, else None.
  - `ROLE_PLAN` — per-type role table: web(structure/design/logic/content/qa), app(ui/backend/logic/data/qa), code(logic/edgecases/optimization/docs/review), general(main/detail/alternatives/polish).
  - `_team_answer` — runs EVERY model in parallel, each assigned ONE role via augmented prompt (ThreadPoolExecutor max_workers=4); collects ALL parts (as_completed, don't cancel); then a TEAM-LEADER model (`_race_first` on synth prompt) merges best of each → one polished Banglish final. Fallback: join parts.
  - `_handle_task` — sends a 🚀 progress msg, starts `_animate_progress` (terminal-style: edits the SAME message line-by-line "Zeron Team activate..." + blinking ▌▐) while models work in thread; on finish cancels animator and edits the same message into the FINAL answer.
  - `_handle_inner` — BEFORE fast-race: if text & not image & detect_task_type → task mode (return). Simple chat/image → fast race (v10).
- Verify: py_compile OK, restarted PID 17840 single process, online logged.
- LESSON (learned/): two-tier design — speed for chat, division-of-labor+merge for tasks. Terminal feel via editing the same Telegram message (line-by-line + blinking cursor) reads as "working" to user without spamming messages.

## Telegram v12 — FULL Telethon feature set (user wish: "sob telethon feature use koro, sob somoy updated")
- User expanded vision: fast chat race + team task mode (v11) PLUS use ALL Telethon/Telegram features; whatever he says should work; always stay updated.
- Added to telegram_action() (core.py now 941 lines):
  - `help` → full command list
  - `my info` → get_me profile card
  - `set bio <text>` / `set name A B` / `set username x` → edit_profile
  - `set profile photo <path>` → upload_file + edit_profile(photo=)
  - `send photo/gif/sticker/file <path>` → send_file
  - `download media @channel` → download_media last media + send back
  - `reaction <emoji> on t.me/...` → send_reaction
  - `pin/unpin <link>` → pin_message/unpin_message
  - `delete message <id> from @ch` → delete_messages
  - `join @ch` / `leave @ch` → join_channel / delete_dialog
  - `create group X` / `create channel X` → create_group/create_channel
  - `my chats` → iter_dialogs summary (user/group/channel counts + top 30)
  - `send poll to @ch : Q | a | b | c` → KeyboardButtonPollType
  - `schedule send to @user : text at HH:MM` → send_message(schedule=...)
  - `search X in @ch` → iter_messages(search=)
  - `battery` → termux-battery-status (Termux:API needed)
- Verify: py_compile OK, restarted PID 23527, online logged.
- OPERATIONAL GOTCHA (learned/): NEVER chain `pkill -9 -f '[c]ore.py'` with a command that also contains literal "core.py" in the same bash -c line (e.g. `nohup python -u core.py`) — the pkill regex matches the shell's OWN command line (contains "core.py") and kills the shell itself (command hangs/terminates). Always: separate call for pkill, separate call for nohup start.

## Telegram v13 — VOICE messages (STT listen + realistic TTS reply)
- User sent voice via Telegram account; wants Zeron to HEAR it, do the task, and reply with a REALISTIC human voice note.
- Installed: edge-tts 7.2.8 (Microsoft neural TTS — bank realistic) + SpeechRecognition 3.17 (Google Speech STT, no pyaudio needed for files). ffmpeg already present.
- faster-whisper FAILED (PyAV 'av' native build fails on Termux arm64). vosk NOT available in this pip index. So STT = Google Speech API (bn-BD then en-US fallback).
- core.py additions:
  - `_clean_speech` strips markdown/emoji so TTS reads clean.
  - `tts(text, afile)` (async) via edge-tts bn-BD-NabanitaNeural (real Bengali female neural voice).
  - `transcribe_voice(wav)` via SpeechRecognition.
  - `_send_out(client, reply, voice)` — sends realistic voice note if voice flag, else text (used everywhere).
  - handler: detects `event.voice` → downloads ogg → ffmpeg to 16k mono wav → asyncio.to_thread(transcribe) → text; sets voice_in; passes through `_handle_inner(..., voice)`.
  - Task mode: if voice, sends short "working" text then final VOICE note (skips text animation).
- Edge-tts learned: /tmp not writable on Android → must write to BASE dir. CLI `--write-media` had a bug; use python SDK `edge-tts.Communicate(...).save()`.

## Telegram v14 — ZeronAssistantBot (bot token 8556525251:AAE...)
- User clarified (custom answer): many features (button messages/keyboards) CANNOT be done on a user account — must be done via a BOT. So build a full PUBLIC bot.
- Bot verified via getMe: id 8556525251, @ZeronAssistantBot.
- Reused entire brain: made `core.py` main guarded with `if __name__=="__main__"` so bot imports all brain functions (race/team/tts/stt/actions) without side effects.
- New `bot.py` (separate process, PID 7725): TelegramClient("zeron_bot", token) 
  - `/start` → MENU_TEXT + InlineKeyboard buttons (Help/Build/My info/Channel/Battery) via Button.inline + CallbackQuery handler.
  - Private text/voice/image → full brain: team mode for tasks, race for chat, voice pitch (tts) in if user sent voice, vision for images.
  - Main bug caught: forgot `await client.run_until_disconnected()` → process exited right after startup.
- NOW 2 daemons running simultaneously: core.py (user account @XASSANNZ) + bot.py (@ZeronAssistantBot). Each own telethon session, no conflict.

## Running processes (2026-08-06)
- core.py PID 7273 — @XASSANNZ (user account, owner DM brain, TELEGRAM->termux/action)
- bot.py PID 7725 — @ZeronAssistantBot (public assistant + buttons)
- START (separate calls, self-match gotcha): `nohup python -u core.py > core.log 2>&1 &` ; `nohup python -u bot.py > bot.log 2>&1 &`
