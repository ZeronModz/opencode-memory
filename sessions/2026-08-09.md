# Session — 2026-08-09 (DevZeron / Hasan)

## Device: Termux (Android) — current phone e kaj. Device file: check devices/.

## 21:20 — GREETING (session start)
User: "hi" — session kickoff. Chatted, ready for work.

## 21:20 — ZERON TEMP MAIL — Brutalist Website Template (NEW PROJECT)
User request: `https://dev-zeron-temp-gmail-api-v1.vercel.app/` er SOB API niye ekta **brutalist** template website. Research kore full updated design, sob API guchiye analyze kore, user shudhu API key change kore publish korbe.

### Ki korlam (worklog)
1. API analyze: `/api/info` theke exact 15 endpoint (auth 3 / generate 5 / read 4 / manage 3 / system 2) + `api/index.py` source protto depuis exact param/response maja. v2 = key-based auth `Authorization: Bearer key_xxxx` (alt X-API-Key).
2. Online research: 2026 brutalist/neo-brutalist trends (tactile brutalism, viewport typo, bento grid, chromatic extremes) + brutalism-web-design skill full design system applied.
3. Design: paper-cream bg #F2EDE4, ink #0b0b0b, red #ff2a00, yellow #ffd60a, zero radius, 3px black borders, hard 6px offset shadows. Archivo Black (display) + Inter (body) + JetBrains Mono (mono). Marquee strip, asymmetric hero with giant type, terminal card, live status dot, bento cards, endpoint matrix grid, dark playground console, register panel, HTTP-code table, security cards, FAQ accordion.
4. Files (folder `~/zeron-temp-mail-brutal-template/`):
   - `index.html` (~35KB) — site
   - `app.js` (~20KB) — logic + **TEMPLATE_CONFIG** (API_BASE + API_KEY) top e
   - `README.md` — publish guide (Vercel/Netlify/GH Pages)
5. Verification:
   - node --check app.js OK
   - HTMLParser: 0 errors, tags balanced
   - JS id cross-check: sob `$("id")` present in HTML
   - VM smoke test e2e: init → endpoint matrix render → playground selects/params (batch=3 fields, register=email+pass) → buildUrl → run() with key from localStorage → fetch → status pill 200 OK + highlighted JSON **PASS**
   - Live API: health 200, info groups match, no-auth 401, bad-key 401, register fake non-gmail → 500 (server bug), fake @gmail → 401 JSON proper
6. **Gotcha found:** live server `/api/register` returns 500 HTML for non-@gmail.com emails (clean_email PermissionError unhandled in register()). Template handles: valid @gmail.com → proper 401 JSON.

### Shikho
- Brutalism 2026: tactile, typography-as-interface, bento grid paradox (mobile collapse), reduced-motion mandated.
- Template publish flow: TEMPLATE_CONFIG e 2 line + static host.

### Files changed
- Created: `~/zeron-temp-mail-brutal-template/{index.html,app.js,README.md}`

## 23:55 — GITHUB + VERCEL DEPLOY (deploy done)
User asked ei 2 step complete korte. Ki korlam:
1. `~/.git-credentials` theke GitHub token ber kore `gh auth login --with-token` → logged in as **ZeronModz**
2. `git init -b main` + commit → `gh repo create zeron-temp-mail-brutal-template --public --source=. --push` → https://github.com/ZeronModz/zeron-temp-mail-brutal-template
3. Vercel CLI issue: symlink `/data/data/com.termux/files/usr/bin/vercel` e shebang `/usr/bin/env node` fail (Termux). Fix: directly run `node /data/data/com.termux/files/usr/lib/node_modules/vercel/dist/vc.js`
4. `vercel deploy --prod --yes` → **Live: https://zeron-temp-mail-brutal-template.vercel.app** (verified 200, content OK)
5. README e one-click deploy button add (Vercel clone button, Netlify deploy button, Railway button) + live demo link. Push korte gh auth setup-git lagche (credential helper nei chilo).
6. Temporary token file dari then wipe.

### Shikho
- Termux e `vercel` CLI bin broken (env shebang) → direct node path diye cholate hobe.
- `gh auth setup-git` push er jonno credential helper set kore dey.
## 23:14 — REBUILD: REAL TEMP MAIL APP (user correction)
User bollen: aage banao ta DOCUMENTARY chilo (API endpoint describe), kintu chai TEMP-MAIL ORG-ER MOTO REAL WEBSITE — normal user inbox e mail dekhe, OTP pore, sob device/sob screen responsive, design brutalist, host e just ENV VAR e API key.

### Ki korlam
1. Research: temp-mail.org features (আগের 403, জ্ঞান থেকে), shieldcn.dev/docs badges format porlam. API live test (register key issue → generate → read → count).
2. Arch: **serverless proxy** `api/index.js` (Vercel Node function, zero-config) — `POST /api` JSON `{path,query}` → env `ZERON_API_KEY` inject kore upstream e forward. Client e key thake NA.
3. `index.html` moobil-first brutalist FULL rewrite (35KB→40KB): hero TEMP_MAIL, address card (COPY/NEW ADDRESS), stats band (msgs/unread/auto/sound toggles), search bar, inbox list→reader pane (desktop 2-col, mobile full-screen overlay), config drawer, toast. Tokens: paper #F2EDE4/ink #0b0b0b/red #ff2a00/yellow #ffd60a/green #12b84a, Archivo Black+Inter+JetBrains Mono, 3px borders, hard shadows, 44px touch, preferred-reduced-motion.
4. `app.js` rewrite: proxy-first + direct-but-key fallback, localStorage (addr/seen/known/auto/sound), generateNew, refreshInbox (silent vs forced), read-track + NEW badge + beep (WebAudio), search via readby, delete, reader, auto-refresh 15s.
5. Env set Vercel: `ZERON_API_KEY=key_EnsATw-vmIygEqHRTkvNAMmX7NNwDrI6BBZxl78lF0E` (production, sensitive).
6. Deployed twice (2nd fix). Live: proxy 200, generate 200, read 200, readby 200, app.js has `$`.
7. README redesigned with shieldcn badges (dynamic json for live API status, github stars/forks/license, static design/stack badges).
8. Git push d101b74. 4 files, +1017/-765.

### Bugs caught (viral)
- encodeURIComponent(email) e `@→%40` → API rejects "Invalid mailbox address" 500. FIX `pathEnc()` — email chars (A-Za-z0-9 . _ @ + -) raw thake, unsafe chars only encode. API + alias e `+` OK.
- Duplicate `function $` remove korte gie TOTAL remove hoye giyechilo (`$ is not defined`) — smoke test dhora. Deployed broken version → redeploy fix.
- `readby/<email>/<text>` text CHARA → 404. Jibon: searchQ empty hole `read/` use.

### Live new API key (registered 23:00-ish, zeronmodz@gmail.com + app pass)
`key_EnsATw-vmIygEqHRTkvNAMmX7NNwDrI6BBZxl78lF0E` — PERMANENT (Firebase), Vercel env `ZERON_API_KEY` e.

### Shikho
- Node v24 e `module._compile` gone → smoke test er jonno require+global stubs.
- shieldcn.svg badge URL: `/badge/{label}-{message}-{color}.svg`, dynamic json `?url=&query=data.status`, github/{stars,forks,license}/{owner}/{repo}.svg.
- Vercel zero-config: static + `api/` function ek saturation e deploy hoy.

## 00:20 — TRUECALLER API RESEARCH (question)
User: Truecaller chin? Free unlimited API paoya jabey? Nahole cookie diye API banaite parbo?
Research on 2026-08-09:
- **No official free unlimited API.** Truecaller er shudhu paid Enterprise API (approval lagbe).
- Unofficial options exist: `truecallerjs`, `truecallerpy`, `nvzard/truecaller-unofficial-api` — reverse-engineered private endpoints + app token/cookie (Premium account lagbe). ToS violation, kono notification chara break hoy, IP/account ban hoy, data = personal (privacy).
- Cookie/token bypass build korte ami decline korlam (access control bypass + personal data scraping). Legit alternatives suggest korlam: Truecaller official paid API, numverify, Twilio Lookup, Abstract Phone Validation, Hiya, Whoscall web.
- Response: declined build, offered legit API alternatives.
