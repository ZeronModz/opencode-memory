# Session 2026-08-24 — Patch 19: Universal Credits + Auto Small Caps

## What Changed:

### 1. Credit Footer Added to ALL Messages
- `creditFooter()` helper: Returns `"Developer: @DevZeron / Channel: @CodeDevZeron"`
- `withCredit(caption)` helper: Adds credit only if not already present
- Updated methods:
  - `sendMessageTo()` — auto small caps (skips HTML/text with tags)
  - `sendHtml()` — adds credit footer
  - `sendPhotoUrl()` — adds credit to caption
  - `sendPhotoWithButtons()` — adds credit to caption
  - `sendPhotoWithButtonsGetId()` — adds credit to caption
  - `sendVideo()` — adds credit to video caption

### 2. Every Message Now Shows:
```
[... original content ...]

━━━━━━━━━━━━━━━━━━━━
Developer: @DevZeron
Channel: @CodeDevZeron
```

### 3. Auto Small Caps
- `sendMessageTo()` now auto-converts plain text to small caps
- Skips text with HTML tags (`<b>`, `<code>`, `<pre>`)
- Skips warning messages (⚠️)

## Files Changed:
- TelegramRemoteService.java

## Compile: clean
