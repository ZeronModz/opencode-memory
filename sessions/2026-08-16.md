# Session — 2026-08-16

## Device
- **Phone:** OPPO CPH2817 (MAIN DEVICE) — Android 16, aarch64
- (Second phone: Samsung A05m)

---

## 06:3X-06:5X — AkashBc auto-follower script update (multi-account + fresh JWT)

**Path:** `/storage/emulated/0/Download/AkashBc/app.py`

### User request
- Pure script e hardcoded ekta JWT chilo; token maeyad kom (7 din) → bar bar new token dewa possible na.
- Chaise: script run korle account.json/access.json path jiggasa korbe → file theke sob account er `uid`+`password` niye sob e `https://vinnyyy-tools.vercel.app/api/uid_to_jwt` POST korbe → response e fresh token → oi token diye follow request (clientbp.ggpolarbear.com/Follow) pathabe — age jevabe, sevabe.
- Terminal e dynamic/sundor output chai: prottek account er success/fail clearly.

### access.json structure
- 636 ta account (concatenated pretty-printed JSON objects, multiple top-level values).
- Sob gulo `{uid, password, account_id, jwt_token}` shape. Shudhu `uid` + `password` dorkar.
- Parser: `json.loads` try → fail hole `JSONDecoder().raw_decode` loop diye sob JSON object alada kora. Clean + robust.

### New script design (app.py rewrite)
- `load_accounts(path)` — BOM-safe (`utf-8-sig`), single object / array / multiple objects sob handle.
- `fetch_token(uid,password)` — fresh JWT API call, **429/5xx e auto-retry (6 attempts, 3*attempt backoff)**, custom Android headers.
- `jwt_expiry(token)` — base64 decode payload → exp → "7h left" style countdown.
- `send_follow(target_id, jwt)` — same as before; returns ok/msg/detail. Success hole `remaining_follow_capacity` + `fail_info` show kore.
- Colorama terminal UI: banner, `[i/total]`, spinner (thread-based) while token fetching, green OK / red FAIL, box-drawing tree output, summary (total/ok/failed + failed UID list).
- Prompts: account file path (default `./access.json`, CLI arg possible) + target UID (default 7842525752).

### Live test results
- fetch_token with retry: 429 lagleo 2nd/3rd attempt e OK.
- JWT expiry ~7h (client version OB54, fresh each fetch).
- Follow request: `{"remaining_follow_capacity": 47, "fail_info": "BR_WORKSHOP_ALREADY_FOLLOWED"}` → status 200 SUCCESS.
- account 6333563049: token OK + follow SUCCESS. 6333560786: 429 persist kore (server rate limit na, ekdom server side).

### Commands
- Run: `cd ~/Download/AkashBc && python3 app.py` → path jiggasa → chole jai.
- deps: requests, colorama, pycryptodome (Crypto), protobuf, follow_pb2.py (custom proto).

### Notes / future
- Vercel API rate limit (1006) proactive handle hoyeche — boro account list eo 429 ashte pare, retry cover kore.
- `fail_info: BR_WORKSHOP_ALREADY_FOLLOWED` mane account tar target age follow koreche (normal).
- follow capacity per account ~47/day limit ache server side.

## 07:0X-07:2X — V2: true follow-success detection + history folder (JSON)

### User correction
- Token generate hoye gele "SUCCESS" dewa UCHIT na. HTTP 200 mane follow hoyeche na.
- **Response parse kore check korte hobe:**
  - `info` present (target account info + nickname) → follow ACTUALLY hoyeche → `FOLLOWED` ✅
  - `fail_info: BR_WORKSHOP_ALREADY_FOLLOWED` → follow hoyni → `NOT FOLLOWED` ❌
- `history/` folder (script er pashe) e sob JSON save: koyta token generate hoyeche, prottek token, follow success/fail, response — sob.

### Changes (app.py)
- `send_follow()` rewrite: status 200 → proto parse → `info` (no fail_info) = `(True, "FOLLOWED", dict)`; `fail_info` present = `(False, "NOT FOLLOWED", dict)`; network/http/parse error = fail.
- `run()` e `history` rec list per account: uid, account_id, target_id, token, token_expiry, follow_success, follow_status, error, response(dict).
- `save_history()` → `history/run_YYYYMMDD_HHMMSS.json`: timestamp, accounts_file, target_id, total, tokens_generated, follows_success, follows_failed, records[]. Creates `history/` next to supplied account file.
- Terminal e follow success dekhate: followed nickname + follower count; fail dekhate fail_info.
- **Test:** account already followed → `NOT FOLLOWED` + fail_info shown correct. save_history JSON valid.

### Note
- `CSFollowRes` proto fields: info, remaining_play_count, remaining_follow_capacity, creator_stats, fail_info.

## 19:0X-19:2X — Switch to OWN local JWT API (no Vercel limi)

### User request
- Age `https://vinnyyy-tools.vercel.app/api/uid_to_jwt` (arekar Vercel) use kora hoitochilo → rate limit kom.
- `jwt/app.py` (nijer full flask API) kothao host na kore **directly locally** use korte chaise.

### What existed
- `jwt/app.py` — Flask API, **2 routes**:
  - `GET /token?uid=<>&password=<>` → OAuth guest token grant → access_token+open_id → MajorLogin (12 platform loop) → JWT. Returns `{token, account_id, account_name, open_id, access_token, platform, region, status}`.
  - `GET /access-jwt?access_token=<>&open_id=<>` → direct MajorLogin JWT.
- `jwt/` e nijer proto (`my_pb2.py`, `output_pb2.py`), AES key/IV, port **1080**.

### Changes (app.py)
- `TOKEN_API` = `https://vinnyyy-tools.vercel.app/api/uid_to_jwt` → **`http://127.0.0.1:1080/token`**.
- `start_jwt_server()` → `jwt/app.py` ko `subprocess.Popen` diye chhotaiche (cwd=jwt, DEVNULL output), port 1080 ready howa porjonto wait.
  - Port already bind hole → error chara **reuse** kore (marbe na).
- `stop_jwt_server(proc)` → normal end / KeyboardInterrupt e terminate.
- `fetch_token()` → vercel POST(json body) theke **GET** (query params `uid`,`password`) → `data["token"]` parse (agra `success` field chilo, ekhon `token` direct). Invalid creds e jwt API 500 → retry cover.
- Lifecycle: `_JWT_SERVER_PROC` module-level; `__main__` try/finally → always stop server.

### Verified
- `jwt/app.py` standalone: port 1080 e Flask runs (fake creds: `/token` → 400 missing / 500 OAuth fail — expected).
- Full run: server auto-start → token fetch → summary → server stop, port closed, **no orphan**.
- Reuse path: server age chile → reuse, no error.
- Dep: `PyJWT` install korte hoyeche (`pip install PyJWT`).
- user note in jwt/app.py tail: “want to see AccountLevel in main code” — pending question.