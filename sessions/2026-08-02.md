# Session — 2026-08-02

## 23:22 — New Project: Zexo (Telegram Bot Client)

**Device**: Same phone as before (Termux Android)

User want kore: Zexo app — apkgen-android-build diye C++ e banao, Material Design 3, colors from `/storage/emulated/0/Download/material-theme.zip`, Telegram bot client (ir.ilmili.telegraph style but for BOT tokens, not accounts). Package `com.devzeron.zexo`.

## What I Built (23:22 → 23:51)
1. **apkgen create zexo --nativecpp** — template nibo (TTY input fail hoye empty package, manually fix korechi)
2. **Project identity**: namespace/appId `com.devzeron.zexo`, app name "Zexo", minSdk 24, targetSdk 34, NDK 29, CMake 4.1.2
3. **M3 theme**: material-theme.zip er values/colors.xml + values-night/colors.xml copy korechi. Theme.Material3.DayNight.NoActionBar base, full color roles (olive/green scheme)
4. **C++ core** (heart):
   - `json.h` — full JSON parser/writer in C++ (zjson namespace, header-only)
   - `tgbot.h/.cpp` — Telegram Bot API client, HTTP via JNI callback → Kotlin HttpBridge
   - `native-lib.cpp` — 15 JNI exports for NativeCore
5. **Kotlin layer**: HttpBridge (HttpURLConnection), NativeCore (JNI decls), Session (prefs), TelegramStore (chats/messages), MainActivity (bottom nav + 8s polling), ChatsFragment, ChatActivity, ProfileFragment, SettingsFragment
6. **Build**: `BUILD SUCCESSFUL` — APK 8.1MB at `Zexo.apk`, libzexo.so all 4 ABIs

## Files Created (zexo project)
```
app/src/main/cpp/json.h, tgbot.h, tgbot.cpp, native-lib.cpp, CMakeLists.txt
app/src/main/kotlin/com/devzeron/zexo/HttpBridge.kt, NativeCore.kt, Session.kt,
  TelegramStore.kt, MainActivity.kt, ChatActivity.kt,
  ChatsFragment.kt, ProfileFragment.kt, SettingsFragment.kt
app/src/main/res/layout/{activity_login,activity_main,activity_chat,fragment_chats,
  fragment_profile,fragment_settings,item_chat,item_message_in,item_message_out,
  item_keyboard_button}.xml
app/src/main/res/drawable/{ic_key,ic_chat,ic_bot,ic_settings,ic_add,ic_attach,ic_send,
  ic_back,bg_avatar,bg_badge}.xml
app/src/main/res/menu/bottom_nav_menu.xml
```

## Build Fixes
- json.h: `Value(true)` → bool ctor add; removed duplicate `long` ctor (== int64_t on Android)
- ChatActivity: `lateinit var chatId: Long` → `var chatId = 0L`

## Output
- `/data/data/com.termux/files/home/Zexo.apk`
- `/storage/emulated/0/Download/Zexo.apk`
- Full details: `projects/zexo-app.md`

## To Do (next session)
- Install/test on device
- v2 features (photo, media multipart, persistence, notifications) — see zexo-app.md

---

## 23:52 → 00:13 (rolled into Aug 3) — Zexo v2: Bugs + Multi-Account + Photos + Actions

**Device**: Same phone (Termux Android)

User reported after testing v1: (1) exit kore abar ashle re-login lage — persist korte hobe, (2) multi-account unlimited, (3) sob persiste hobe (profile pic, chats, info), (4) chat profiles like Telegram (avatar, name, everything), (5) incoming messages ashe na — bug fix, (6) theme colors sob jaygay na (bottom nav etc), (7) status bar icon color thik nai, (8) icon issues, (9) online research → Telegram Bot API docs, (10) Vercel API build korte hobe.

### v2 Built (all done, BUILD SUCCESSFUL)
1. **Research**: Bot API 10.2 — getUserProfilePhotos, getFile, sendPhoto (URL/file_id), forwardMessage, copyMessage, sendChatAction, pin/unpinChatMessage, ban/unbanChatMember, leaveChat, deleteMessages, getChatMember, getChatAdministrators, setMyProfilePhoto, setChatTitle
2. **C++ core**: tgbot.h/.cpp + native-lib.cpp — all new methods + JNI exports
3. **NativeCore.kt**: all new externals (incl. nativeDeleteMessages(LongArray))
4. **HttpBridge.kt**: + `downloadToFile(url, savePath)` and `uploadMultipart(url, fields, fileField, filePath, fileName, contentType)`
5. **Session.kt** → `AccountManager` (multi-account in SharedPreferences) + legacy Session shim
6. **TelegramStore.kt** → disk persistence (`filesDir/accounts/<botId>/data.json`), switchTo, avatar cache, MessageData+photoFileId/caption/fromId, upsertSentPhoto, deleteMessagesBulk
7. **UpdatePoller.kt** — singleton poller (single thread, 5s interval, 15s backoff) — FIXES missing incoming messages (was multiple racing pollers)
8. **AvatarLoader.kt** — profile photo download → circle crop → save → callback
9. **SplashActivity.kt** — 600ms, routes to Main/Login by stored account — FIXES re-login
10. **MainActivity.kt** — account bar (avatar + name) + account switcher dialog
11. **ChatsFragment/Adapter + item_chat.xml** — avatars in chat list
12. **ChatActivity.kt** — photo attach (gallery → uploadMultipart sendPhoto), reply/copy/forward/pin/edit/delete long-press menu, ChatProfile navigation
13. **ChatProfileActivity.kt** — chat info (avatar, name, username, type/members chips, bio, ban/leave)
14. **ProfileFragment.kt** — accounts list (AccountsAdapter + item_account.xml), add account, set profile photo (setMyProfilePhoto via multipart), edit dialog 5 options
15. **SettingsFragment.kt** — logout via AccountManager + deleteRecursively; webhook info from current account
16. **Manifest** — SplashActivity = launcher; Login/Main/Chat/ChatProfile registered
17. **strings.xml** — all v2 strings added
18. **themes.xml** — windowLightStatusBar (+night false), nav bar surface color; `ShapeAppearanceOverlay.Material3.Corner.Full`
19. **Icons** — fixed ic_bot (proper Material robot), ic_chat fixed

### v2 Build Fixes
- `activity_splash.xml` missing `xmlns:app` → resource link error
- missing `@color/white` (added to values + values-night)
- missing `ShapeAppearanceOverlay.Material3.Corner.Full` style (added)
- `AvatarLoader.Callback` → `fun interface` (SAM) so lambdas work
- removed stale `TelegramStore.setBotId` call in LoginActivity

### Output
- APK 9.7MB: `~/Zexo.apk` + `/storage/emulated/0/Download/Zexo.apk`
- Full v2 details: `projects/zexo-app.md`

## To Do (v3)
- Install/test v2 on device
- Vercel API (proxy/webhook cache) — not started
- Notifications, background poller
- voice/video/document media, topics/reply threads
- Custom app icon
